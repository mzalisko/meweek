# План імплементації 1.2 — DataBridge і доставка

> **✅ ВИКОНАНО 2026-06-14.** Tasks 1–11 реалізовано й закомічено. Прогони: core 49, bridge 24 (22 за планом + 2 security-тести на fail-closed serve/пінг, додані під час закриття за результатом рев'ю). Міграції на живих MySQL — без помилок. Доперевірка (рев'ю не дійшло до кінця через ліміт сесії): див. «Доперевірити» у `DBManager — Контекст.md`.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Окремий публічний сервіс DataBridge, що приймає опубліковані дані від Core (HMAC), віддає їх WP-сайтам по токену з підписом і версією-ETag, шле push-пінги з ретраями; плюс Core-сторона: токени сайтів і публікатор у bridge.

**Architecture:** Другий Laravel-додаток `bridge/` із власною БД (`bridge-mysql`) — він НІКОЛИ не звертається до БД Core (ізоляція). Core після `SitePayloadCompiler::publish()` пушить payload у bridge через внутрішній HMAC-захищений ендпоінт; bridge зберігає read-only копію, віддає її сайтам (`GET /v1/data` по токену) з `X-Signature` і `ETag`, підтримує звірку версії (`If-None-Match` → 304) і черговий push-пінг із backoff. Контракт payload — той самий, що компілює Core у плані 1.1: `{site, version, generated_at, values[]}`.

**Tech Stack:** PHP 8.4, Laravel 13, MySQL 8.4 (окрема БД для bridge), Redis, Docker Compose, PHPUnit, Http::fake/Queue::fake для тестів доставки.

**Спека:** `10-Projects/DBManager/DBManager — Дизайн.md` (§3 архітектура, §5 доставка, §9 безпека). Попередній план: `plans/2026-06-11 — План 1.1 — Core.md` (Core готовий, 42 тести).

**Робоча директорія docker-команд:** `C:/Dev/Meweek/10-Projects/DBManager/code` (bash). Git — з кореня репо `C:/Dev/Meweek`, коміти українською `dbmanager: ...`, тільки шляхи під `10-Projects/DBManager/code`, без push (крім фінального таска).

**Узгодження хешування токена (критично):** і Core (`SiteProvisioner`), і Bridge (serve) хешують сирий токен як `hash('sha256', $rawToken)`. Обидві сторони мають використати РІВНО це.

---

### Task 1: DataBridge — Laravel skeleton + compose-сервіс

**Files:**
- Modify: `10-Projects/DBManager/code/docker-compose.yml`
- Create: `10-Projects/DBManager/code/bridge/` (Laravel via composer)
- Modify: `bridge/phpunit.xml`, `bridge/tests/TestCase.php`
- Modify: `bridge/.env.example`

- [ ] **Step 1: Додати сервіси bridge і bridge-mysql у docker-compose.yml**

Додати в секцію `services:` (поряд з core/mysql/redis), і новий том:
```yaml
  bridge:
    build: ./docker/core
    volumes:
      - ./bridge:/app
    ports:
      - "8002:8000"
    environment:
      DB_CONNECTION: mysql
      DB_HOST: bridge-mysql
      DB_PORT: "3306"
      DB_DATABASE: dbmanager_bridge
      DB_USERNAME: root
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      REDIS_HOST: redis
    depends_on:
      - bridge-mysql
      - redis

  bridge-mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: dbmanager_bridge
    volumes:
      - bridge-mysql-data:/var/lib/mysql
    ports:
      - "33062:3306"
```
І в `volumes:` додати `bridge-mysql-data:`. Образ той самий (`./docker/core`) — він узагальнений PHP-CLI, перевикористання навмисне.

- [ ] **Step 2: Створити Laravel-проєкт bridge**

Run (з `code/`):
```bash
docker run --rm -v "$PWD:/work" -w /work composer:2 create-project laravel/laravel bridge
```
Expected: папка `bridge/` з Laravel 13, згенерований `bridge/.env` з APP_KEY.

- [ ] **Step 3: Тестова конфігурація (sqlite :memory:)**

У `bridge/phpunit.xml` у секції `<php>` переконатися, що є (розкоментувати/додати):
```xml
<env name="DB_CONNECTION" value="sqlite"/>
<env name="DB_DATABASE" value=":memory:"/>
```

`bridge/tests/TestCase.php` лишити стандартним (без `$seed` — bridge не має сидерів).

- [ ] **Step 4: Ключі середовища**

У `bridge/.env.example` додати:
```dotenv
BRIDGE_PUBLISH_SECRET=
BRIDGE_DATA_SIGNING_SECRET=
BRIDGE_PING_SECRET=
```

- [ ] **Step 5: Зібрати й перевірити стартові тести**

Run:
```bash
docker compose build bridge && docker compose run --rm bridge php artisan test
```
Expected: `Tests: 2 passed` (стандартні ExampleTest).

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code
git commit -m "dbmanager: каркас сервісу DataBridge у docker"
```

---

### Task 2: Bridge — модель даних (published_sites, request_logs)

**Files:**
- Create: `bridge/database/migrations/2026_06_13_000001_create_bridge_tables.php`
- Create: `bridge/app/Models/PublishedSite.php`, `bridge/app/Models/RequestLog.php`
- Create: `bridge/database/factories/PublishedSiteFactory.php`
- Test: `bridge/tests/Feature/BridgeModelTest.php`

- [ ] **Step 1: Написати тест**

`bridge/tests/Feature/BridgeModelTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class BridgeModelTest extends TestCase
{
    use RefreshDatabase;

    public function test_published_site_stores_payload_and_version(): void
    {
        $site = PublishedSite::factory()->create([
            'domain' => 'domen.ua',
            'version' => 7,
            'payload' => ['site' => 'domen.ua', 'version' => 7, 'values' => []],
        ]);

        $fresh = $site->fresh();
        $this->assertSame(7, $fresh->version);
        $this->assertSame('domen.ua', $fresh->payload['site']);
    }

    public function test_token_hash_is_unique(): void
    {
        PublishedSite::factory()->create(['token_hash' => 'abc']);

        $this->expectException(\Illuminate\Database\QueryException::class);
        PublishedSite::factory()->create(['token_hash' => 'abc']);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=BridgeModelTest`
Expected: FAIL.

- [ ] **Step 3: Міграція**

`bridge/database/migrations/2026_06_13_000001_create_bridge_tables.php`:
```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('published_sites', function (Blueprint $table) {
            $table->id();
            $table->string('domain')->unique();
            $table->string('token_hash')->unique();
            $table->string('ping_url')->nullable();
            $table->unsignedBigInteger('version')->default(0);
            $table->json('payload');
            $table->timestamps();
            $table->index('token_hash', 'published_sites_token_lookup');
        });

        Schema::create('request_logs', function (Blueprint $table) {
            $table->id();
            $table->string('token_hash')->nullable();
            $table->string('ip', 45)->nullable();
            $table->string('path');
            $table->unsignedSmallInteger('status');
            $table->timestamp('created_at')->useCurrent();
            $table->index(['token_hash', 'created_at']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('request_logs');
        Schema::dropIfExists('published_sites');
    }
};
```

- [ ] **Step 4: Моделі**

`bridge/app/Models/PublishedSite.php`:
```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class PublishedSite extends Model
{
    use HasFactory;

    protected $guarded = [];

    protected $casts = ['payload' => 'array'];
}
```

`bridge/app/Models/RequestLog.php`:
```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class RequestLog extends Model
{
    public $timestamps = false;

    protected $guarded = [];

    protected $casts = ['created_at' => 'datetime'];
}
```

- [ ] **Step 5: Фабрика**

`bridge/database/factories/PublishedSiteFactory.php`:
```php
<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Str;

class PublishedSiteFactory extends Factory
{
    public function definition(): array
    {
        $domain = fake()->unique()->domainName();

        return [
            'domain' => $domain,
            'token_hash' => hash('sha256', Str::random(48)),
            'ping_url' => 'https://'.$domain.'/wp-json/dbm/v1/ping',
            'version' => 1,
            'payload' => ['site' => $domain, 'version' => 1, 'values' => []],
        ];
    }
}
```

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=BridgeModelTest`
Expected: PASS (2 tests).
Then: `docker compose run --rm bridge php artisan test`
Expected: 4 passed.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): модель опублікованих сайтів і журнал запитів"
```

---

### Task 3: Bridge — ingest-ендпоінт (Core → Bridge, HMAC)

**Files:**
- Create: `bridge/routes/api.php`
- Modify: `bridge/bootstrap/app.php`
- Modify: `bridge/config/services.php`
- Create: `bridge/app/Http/Controllers/Api/IngestController.php`
- Create: `bridge/app/Jobs/DeliverPingJob.php` (заглушка-каркас; повна логіка — Task 7)
- Test: `bridge/tests/Feature/IngestTest.php`

- [ ] **Step 1: Написати тести**

`bridge/tests/Feature/IngestTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Jobs\DeliverPingJob;
use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Queue;
use Tests\TestCase;

class IngestTest extends TestCase
{
    use RefreshDatabase;

    private const SECRET = 'publish-secret';

    protected function setUp(): void
    {
        parent::setUp();
        config(['services.publish.secret' => self::SECRET]);
    }

    private function body(array $over = []): array
    {
        return array_merge([
            'domain' => 'domen.ua',
            'token_hash' => hash('sha256', 'raw-token'),
            'ping_url' => 'https://domen.ua/wp-json/dbm/v1/ping',
            'version' => 1,
            'payload' => ['site' => 'domen.ua', 'version' => 1, 'values' => []],
        ], $over);
    }

    private function postSigned(array $body, ?string $secret = null)
    {
        $json = json_encode($body);

        return $this->call('POST', '/api/internal/publish', [], [], [], [
            'CONTENT_TYPE' => 'application/json',
            'HTTP_X_SIGNATURE' => hash_hmac('sha256', $json, $secret ?? self::SECRET),
            'HTTP_ACCEPT' => 'application/json',
        ], $json);
    }

    public function test_invalid_signature_rejected(): void
    {
        Queue::fake();
        $this->postSigned($this->body(), 'wrong')->assertStatus(401);
        $this->assertSame(0, PublishedSite::count());
    }

    public function test_ingest_stores_site_and_dispatches_ping(): void
    {
        Queue::fake();

        $this->postSigned($this->body())->assertOk();

        $site = PublishedSite::where('domain', 'domen.ua')->first();
        $this->assertNotNull($site);
        $this->assertSame(1, $site->version);
        $this->assertSame('domen.ua', $site->payload['site']);
        Queue::assertPushed(DeliverPingJob::class);
    }

    public function test_ingest_is_monotonic_ignores_older_version(): void
    {
        Queue::fake();
        $this->postSigned($this->body(['version' => 5,
            'payload' => ['site' => 'domen.ua', 'version' => 5, 'values' => []]]))->assertOk();

        // старіша версія — ігнорується (не відкочує), відповідь 409
        $this->postSigned($this->body(['version' => 3,
            'payload' => ['site' => 'domen.ua', 'version' => 3, 'values' => []]]))
            ->assertStatus(409);

        $this->assertSame(5, PublishedSite::where('domain', 'domen.ua')->first()->version);
    }

    public function test_ingest_updates_existing_to_newer_version(): void
    {
        Queue::fake();
        $this->postSigned($this->body(['version' => 1]))->assertOk();
        $this->postSigned($this->body(['version' => 2,
            'payload' => ['site' => 'domen.ua', 'version' => 2, 'values' => []]]))->assertOk();

        $this->assertSame(2, PublishedSite::where('domain', 'domen.ua')->first()->version);
        $this->assertSame(1, PublishedSite::where('domain', 'domen.ua')->count());
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=IngestTest`
Expected: FAIL (404).

- [ ] **Step 3: Заглушка джоби (щоб Queue::fake бачив клас)**

`bridge/app/Jobs/DeliverPingJob.php`:
```php
<?php

namespace App\Jobs;

use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;

class DeliverPingJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    public function __construct(public int $publishedSiteId) {}

    public function handle(): void
    {
        // Повна логіка пінга з backoff — Task 7
    }
}
```

- [ ] **Step 4: Маршрут, конфіг, реєстрація API**

`bridge/routes/api.php`:
```php
<?php

use App\Http\Controllers\Api\IngestController;
use Illuminate\Support\Facades\Route;

Route::post('/internal/publish', IngestController::class);
```

У `bridge/bootstrap/app.php` додати `api:` у `withRouting` (зберігши наявні параметри):
```php
->withRouting(
    web: __DIR__.'/../routes/web.php',
    api: __DIR__.'/../routes/api.php',
    commands: __DIR__.'/../routes/console.php',
    health: '/up',
)
```

У `bridge/config/services.php` додати:
```php
'publish' => [
    'secret' => env('BRIDGE_PUBLISH_SECRET'),
],
'data' => [
    'signing_secret' => env('BRIDGE_DATA_SIGNING_SECRET'),
],
'ping' => [
    'secret' => env('BRIDGE_PING_SECRET'),
],
```

- [ ] **Step 5: Контролер**

`bridge/app/Http/Controllers/Api/IngestController.php`:
```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Jobs\DeliverPingJob;
use App\Models\PublishedSite;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class IngestController extends Controller
{
    public function __invoke(Request $request): JsonResponse
    {
        $secret = config('services.publish.secret');
        abort_if(! $secret, 500, 'Publish secret is not configured');

        $expected = hash_hmac('sha256', $request->getContent(), $secret);
        abort_unless(
            hash_equals($expected, (string) $request->header('X-Signature')),
            401,
            'Invalid signature'
        );

        $data = $request->validate([
            'domain' => ['required', 'string', 'max:255'],
            'token_hash' => ['required', 'string', 'max:64'],
            'ping_url' => ['nullable', 'string', 'max:255'],
            'version' => ['required', 'integer', 'min:0'],
            'payload' => ['required', 'array'],
        ]);

        $existing = PublishedSite::where('domain', $data['domain'])->first();

        if ($existing && $data['version'] <= $existing->version) {
            return response()->json(['message' => 'Stale version'], 409);
        }

        $site = PublishedSite::updateOrCreate(
            ['domain' => $data['domain']],
            [
                'token_hash' => $data['token_hash'],
                'ping_url' => $data['ping_url'],
                'version' => $data['version'],
                'payload' => $data['payload'],
            ]
        );

        DeliverPingJob::dispatch($site->id);

        return response()->json(['stored_version' => $site->version]);
    }
}
```

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=IngestTest`
Expected: PASS (4 tests).
Then: `docker compose run --rm bridge php artisan test`
Expected: 8 passed.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): прийом публікацій від Core з HMAC і монотонністю версій"
```

---

### Task 4: Bridge — serve-ендпоінт (сайт → дані по токену, підпис)

**Files:**
- Modify: `bridge/routes/api.php`
- Create: `bridge/app/Http/Controllers/Api/DataController.php`
- Test: `bridge/tests/Feature/DataServeTest.php`

- [ ] **Step 1: Написати тести**

`bridge/tests/Feature/DataServeTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class DataServeTest extends TestCase
{
    use RefreshDatabase;

    private const SIGN = 'data-signing-secret';

    protected function setUp(): void
    {
        parent::setUp();
        config(['services.data.signing_secret' => self::SIGN]);
    }

    private function makeSite(string $rawToken, array $over = []): PublishedSite
    {
        return PublishedSite::factory()->create(array_merge([
            'domain' => 'domen.ua',
            'token_hash' => hash('sha256', $rawToken),
            'version' => 4,
            'payload' => ['site' => 'domen.ua', 'version' => 4, 'values' => [['key' => 'phone_ua_1']]],
        ], $over));
    }

    private function get(string $rawToken, array $headers = [])
    {
        return $this->getJson('/api/v1/data', array_merge([
            'X-Site-Token' => $rawToken,
        ], $headers));
    }

    public function test_unknown_token_is_unauthorized(): void
    {
        $this->makeSite('good-token');

        $this->get('bad-token')->assertStatus(401);
    }

    public function test_missing_token_is_unauthorized(): void
    {
        $this->getJson('/api/v1/data')->assertStatus(401);
    }

    public function test_valid_token_returns_signed_payload_with_etag(): void
    {
        $site = $this->makeSite('good-token');

        $response = $this->get('good-token')->assertOk();

        $response->assertHeader('ETag', '"4"');
        $body = $response->getContent();
        $expectedSig = hash_hmac('sha256', $body, self::SIGN);
        $response->assertHeader('X-Signature', $expectedSig);

        $json = $response->json();
        $this->assertSame('domen.ua', $json['site']);
        $this->assertSame(4, $json['version']);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=DataServeTest`
Expected: FAIL (404).

- [ ] **Step 3: Маршрут**

У `bridge/routes/api.php` додати (поряд із internal/publish):
```php
use App\Http\Controllers\Api\DataController;

Route::get('/v1/data', DataController::class);
```

- [ ] **Step 4: Контролер**

`bridge/app/Http/Controllers/Api/DataController.php`:
```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\PublishedSite;
use Illuminate\Http\Request;
use Illuminate\Http\Response;

class DataController extends Controller
{
    public function __invoke(Request $request): Response
    {
        $rawToken = (string) $request->header('X-Site-Token');
        abort_if($rawToken === '', 401, 'Missing token');

        $site = PublishedSite::where('token_hash', hash('sha256', $rawToken))->first();
        abort_unless($site, 401, 'Unknown token');

        $etag = '"'.$site->version.'"';

        if (trim((string) $request->header('If-None-Match')) === $etag) {
            return response('', 304)->header('ETag', $etag);
        }

        $body = json_encode($site->payload, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES);
        $signature = hash_hmac('sha256', $body, (string) config('services.data.signing_secret'));

        return response($body, 200)
            ->header('Content-Type', 'application/json')
            ->header('ETag', $etag)
            ->header('X-Signature', $signature);
    }
}
```

- [ ] **Step 5: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=DataServeTest`
Expected: PASS (3 tests).
Then: `docker compose run --rm bridge php artisan test`
Expected: 11 passed.

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): видача даних по токену з підписом і ETag"
```

---

### Task 5: Bridge — звірка версії (If-None-Match → 304)

**Files:**
- Test: `bridge/tests/Feature/ReconciliationTest.php`
(Логіка 304 вже в DataController з Task 4 — цей таск її ФІКСУЄ тестами звірки парку.)

- [ ] **Step 1: Написати тести**

`bridge/tests/Feature/ReconciliationTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class ReconciliationTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config(['services.data.signing_secret' => 'sign']);
    }

    private function site(): PublishedSite
    {
        return PublishedSite::factory()->create([
            'token_hash' => hash('sha256', 'tok'),
            'version' => 9,
            'payload' => ['site' => 'd.ua', 'version' => 9, 'values' => []],
        ]);
    }

    public function test_matching_version_returns_304_without_body(): void
    {
        $this->site();

        $response = $this->getJson('/api/v1/data', [
            'X-Site-Token' => 'tok',
            'If-None-Match' => '"9"',
        ]);

        $response->assertStatus(304);
        $this->assertSame('', $response->getContent());
        $response->assertHeader('ETag', '"9"');
    }

    public function test_stale_version_returns_200_with_fresh_payload(): void
    {
        $this->site();

        $response = $this->getJson('/api/v1/data', [
            'X-Site-Token' => 'tok',
            'If-None-Match' => '"7"',
        ]);

        $response->assertOk();
        $this->assertSame(9, $response->json('version'));
    }
}
```

- [ ] **Step 2: Прогнати — очікувано зелений (логіка вже є)**

Run: `docker compose run --rm bridge php artisan test --filter=ReconciliationTest`
Expected: PASS (2 tests). Якщо червоне — діагностуй DataController (304-гілку), виправ контролер, не тести.
Then: `docker compose run --rm bridge php artisan test`
Expected: 13 passed.

- [ ] **Step 3: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): звірка версії сайтів через If-None-Match"
```

---

### Task 6: Bridge — rate limiting і журналювання запитів

**Files:**
- Modify: `bridge/routes/api.php` (throttle на serve)
- Create: `bridge/app/Http/Middleware/LogBridgeRequest.php`
- Modify: `bridge/bootstrap/app.php` (зареєструвати alias middleware)
- Test: `bridge/tests/Feature/RateLimitAndLogTest.php`

- [ ] **Step 1: Написати тести**

`bridge/tests/Feature/RateLimitAndLogTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\PublishedSite;
use App\Models\RequestLog;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class RateLimitAndLogTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config(['services.data.signing_secret' => 'sign']);
    }

    public function test_serve_request_is_logged(): void
    {
        PublishedSite::factory()->create([
            'token_hash' => hash('sha256', 'tok'), 'version' => 1,
            'payload' => ['site' => 'd.ua', 'version' => 1, 'values' => []],
        ]);

        $this->getJson('/api/v1/data', ['X-Site-Token' => 'tok'])->assertOk();

        $log = RequestLog::where('path', 'api/v1/data')->first();
        $this->assertNotNull($log);
        $this->assertSame(200, $log->status);
        $this->assertSame(hash('sha256', 'tok'), $log->token_hash);
    }

    public function test_unauthorized_request_is_logged_with_401(): void
    {
        $this->getJson('/api/v1/data', ['X-Site-Token' => 'nope'])->assertStatus(401);

        $this->assertTrue(RequestLog::where('path', 'api/v1/data')->where('status', 401)->exists());
    }

    public function test_serve_is_rate_limited(): void
    {
        PublishedSite::factory()->create([
            'token_hash' => hash('sha256', 'tok'), 'version' => 1,
            'payload' => ['site' => 'd.ua', 'version' => 1, 'values' => []],
        ]);

        $hit429 = false;
        for ($i = 0; $i < 125; $i++) {
            $status = $this->getJson('/api/v1/data', ['X-Site-Token' => 'tok'])->status();
            if ($status === 429) {
                $hit429 = true;
                break;
            }
        }
        $this->assertTrue($hit429, 'Очікували 429 у межах ліміту');
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=RateLimitAndLogTest`
Expected: FAIL (нема логів / нема ліміту).

- [ ] **Step 3: Middleware журналювання**

`bridge/app/Http/Middleware/LogBridgeRequest.php`:
```php
<?php

namespace App\Http\Middleware;

use App\Models\RequestLog;
use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class LogBridgeRequest
{
    public function handle(Request $request, Closure $next): Response
    {
        $response = $next($request);

        $rawToken = (string) $request->header('X-Site-Token');

        RequestLog::create([
            'token_hash' => $rawToken !== '' ? hash('sha256', $rawToken) : null,
            'ip' => $request->ip(),
            'path' => $request->path(),
            'status' => $response->getStatusCode(),
        ]);

        return $response;
    }
}
```

- [ ] **Step 4: Зареєструвати alias і застосувати throttle**

У `bridge/bootstrap/app.php` у `->withMiddleware(function (Middleware $middleware) { ... })` додати alias:
```php
$middleware->alias([
    'bridge.log' => \App\Http\Middleware\LogBridgeRequest::class,
]);
```
(Якщо замикання middleware порожнє — додати цей виклик; зберегти наявне.)

У `bridge/routes/api.php` обгорнути serve-маршрут:
```php
Route::get('/v1/data', DataController::class)
    ->middleware(['bridge.log', 'throttle:120,1']);
```
(ingest лишається без throttle — він внутрішній і захищений HMAC.)

- [ ] **Step 5: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=RateLimitAndLogTest`
Expected: PASS (3 tests).
Then: `docker compose run --rm bridge php artisan test`
Expected: 16 passed.

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): rate limiting і журналювання запитів serve"
```

---

### Task 7: Bridge — push-пінг із backoff (DeliverPingJob)

**Files:**
- Modify: `bridge/app/Jobs/DeliverPingJob.php` (повна логіка)
- Test: `bridge/tests/Feature/DeliverPingTest.php`

- [ ] **Step 1: Написати тести**

`bridge/tests/Feature/DeliverPingTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Jobs\DeliverPingJob;
use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Http;
use Tests\TestCase;

class DeliverPingTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config(['services.ping.secret' => 'ping-secret']);
    }

    public function test_ping_posts_signed_payload_to_site(): void
    {
        Http::fake(['*' => Http::response('', 200)]);
        $site = PublishedSite::factory()->create([
            'domain' => 'domen.ua',
            'ping_url' => 'https://domen.ua/wp-json/dbm/v1/ping',
            'version' => 5,
        ]);

        (new DeliverPingJob($site->id))->handle();

        Http::assertSent(function ($request) {
            $body = $request->body();
            $expected = hash_hmac('sha256', $body, 'ping-secret');

            return $request->url() === 'https://domen.ua/wp-json/dbm/v1/ping'
                && $request->hasHeader('X-Signature', $expected)
                && json_decode($body, true)['version'] === 5
                && json_decode($body, true)['domain'] === 'domen.ua';
        });
    }

    public function test_ping_without_url_is_noop(): void
    {
        Http::fake();
        $site = PublishedSite::factory()->create(['ping_url' => null]);

        (new DeliverPingJob($site->id))->handle();

        Http::assertNothingSent();
    }

    public function test_failed_ping_throws_for_retry(): void
    {
        Http::fake(['*' => Http::response('', 500)]);
        $site = PublishedSite::factory()->create([
            'ping_url' => 'https://domen.ua/ping', 'version' => 1,
        ]);

        $this->expectException(\RuntimeException::class);
        (new DeliverPingJob($site->id))->handle();
    }

    public function test_backoff_schedule_is_exponential(): void
    {
        $job = new DeliverPingJob(1);

        $this->assertSame([60, 300, 1800, 7200, 21600, 43200, 86400], $job->backoff());
        $this->assertSame(8, $job->tries);
    }

    public function test_missing_site_is_noop(): void
    {
        Http::fake();

        (new DeliverPingJob(999999))->handle();

        Http::assertNothingSent();
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=DeliverPingTest`
Expected: FAIL (заглушка нічого не робить).

- [ ] **Step 3: Повна логіка джоби**

`bridge/app/Jobs/DeliverPingJob.php` (повністю замінити):
```php
<?php

namespace App\Jobs;

use App\Models\PublishedSite;
use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;
use Illuminate\Support\Facades\Http;

class DeliverPingJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    public int $tries = 8;

    public function __construct(public int $publishedSiteId) {}

    /** Наростаючий backoff: 1хв → 5хв → 30хв → 2г → 6г → 12г → 24г. */
    public function backoff(): array
    {
        return [60, 300, 1800, 7200, 21600, 43200, 86400];
    }

    public function handle(): void
    {
        $site = PublishedSite::find($this->publishedSiteId);

        if (! $site || ! $site->ping_url) {
            return; // сайт зник або не має URL для пінга — нічого робити
        }

        $body = json_encode([
            'domain' => $site->domain,
            'version' => $site->version,
        ], JSON_UNESCAPED_SLASHES);

        $signature = hash_hmac('sha256', $body, (string) config('services.ping.secret'));

        $response = Http::withHeaders([
            'Content-Type' => 'application/json',
            'X-Signature' => $signature,
        ])->withBody($body, 'application/json')->post($site->ping_url);

        if ($response->failed()) {
            throw new \RuntimeException("Ping failed for {$site->domain}: HTTP {$response->status()}");
        }
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=DeliverPingTest`
Expected: PASS (5 tests).
Then: `docker compose run --rm bridge php artisan test`
Expected: 21 passed.

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): push-пінг сайтам з HMAC і backoff-ретраями"
```

---

### Task 8: Core — токени сайтів (ping_url + SiteProvisioner)

**Files:**
- Create: `core/database/migrations/2026_06_13_000001_add_ping_url_to_sites.php`
- Create: `core/app/Services/Provisioning/SiteProvisioner.php`
- Create: `core/database/factories/ApiTokenFactory.php`
- Modify: `core/app/Models/ApiToken.php` (use HasFactory)
- Test: `core/tests/Feature/SiteProvisionerTest.php`

- [ ] **Step 1: Написати тести**

`core/tests/Feature/SiteProvisionerTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\ApiToken;
use App\Models\Site;
use App\Services\Provisioning\SiteProvisioner;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class SiteProvisionerTest extends TestCase
{
    use RefreshDatabase;

    public function test_issue_token_returns_raw_and_stores_hash(): void
    {
        $site = Site::factory()->create();

        $raw = app(SiteProvisioner::class)->issueToken($site, 'основний');

        $this->assertNotEmpty($raw);
        $token = ApiToken::where('site_id', $site->id)->first();
        $this->assertSame(hash('sha256', $raw), $token->token_hash);
        $this->assertSame('основний', $token->label);
        $this->assertNull($token->revoked_at);
    }

    public function test_active_token_hash_returns_latest_non_revoked(): void
    {
        $site = Site::factory()->create();
        $provisioner = app(SiteProvisioner::class);

        $first = $provisioner->issueToken($site);
        ApiToken::where('site_id', $site->id)->update(['revoked_at' => now()]);
        $second = $provisioner->issueToken($site);

        $this->assertSame(hash('sha256', $second), $provisioner->activeTokenHash($site));
    }

    public function test_active_token_hash_null_when_none(): void
    {
        $site = Site::factory()->create();

        $this->assertNull(app(SiteProvisioner::class)->activeTokenHash($site));
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=SiteProvisionerTest`
Expected: FAIL.

- [ ] **Step 3: Міграція (ping_url на sites)**

`core/database/migrations/2026_06_13_000001_add_ping_url_to_sites.php`:
```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('sites', function (Blueprint $table) {
            $table->string('ping_url')->nullable()->after('country_hint');
        });
    }

    public function down(): void
    {
        Schema::table('sites', function (Blueprint $table) {
            $table->dropColumn('ping_url');
        });
    }
};
```

- [ ] **Step 4: ApiToken — фабрика + HasFactory**

У `core/app/Models/ApiToken.php` додати трейт (до класу): `use Illuminate\Database\Eloquent\Factories\HasFactory;` і `use HasFactory;` у тілі (поряд із наявним). Решту моделі не чіпати.

`core/database/factories/ApiTokenFactory.php`:
```php
<?php

namespace Database\Factories;

use App\Models\Site;
use Illuminate\Database\Eloquent\Factories\Factory;
use Illuminate\Support\Str;

class ApiTokenFactory extends Factory
{
    public function definition(): array
    {
        return [
            'site_id' => Site::factory(),
            'token_hash' => hash('sha256', Str::random(48)),
            'label' => null,
            'revoked_at' => null,
        ];
    }
}
```

- [ ] **Step 5: Сервіс**

`core/app/Services/Provisioning/SiteProvisioner.php`:
```php
<?php

namespace App\Services\Provisioning;

use App\Models\ApiToken;
use App\Models\Site;
use Illuminate\Support\Str;

class SiteProvisioner
{
    /** Згенерувати токен сайта; повертає СИРИЙ токен один раз (у БД лише хеш). */
    public function issueToken(Site $site, ?string $label = null): string
    {
        $raw = Str::random(48);

        ApiToken::create([
            'site_id' => $site->id,
            'token_hash' => hash('sha256', $raw),
            'label' => $label,
        ]);

        return $raw;
    }

    /** Хеш останнього чинного (невідкликаного) токена сайта або null. */
    public function activeTokenHash(Site $site): ?string
    {
        return ApiToken::where('site_id', $site->id)
            ->whereNull('revoked_at')
            ->latest('id')
            ->value('token_hash');
    }
}
```

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm core php artisan test --filter=SiteProvisionerTest`
Expected: PASS (3 tests).
Then: `docker compose run --rm core php artisan test`
Expected: 45 passed (42 + 3).

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager: токени сайтів і ping_url у Core"
```

---

### Task 9: Core — публікатор у Bridge (BridgePublisher) + інтеграція

**Files:**
- Modify: `core/config/services.php` (bridge ingest url + publish secret)
- Modify: `core/.env.example`
- Create: `core/app/Services/Publishing/BridgePublisher.php`
- Modify: `core/app/Http/Controllers/Api/MonitoringWebhookController.php` (виклик публікатора після транзакції)
- Test: `core/tests/Feature/BridgePublisherTest.php`
- Test: `core/tests/Feature/WebhookPublishesToBridgeTest.php`

- [ ] **Step 1: Написати тести**

`core/tests/Feature/BridgePublisherTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\Site;
use App\Services\Provisioning\SiteProvisioner;
use App\Services\Publishing\BridgePublisher;
use App\Services\Publishing\SitePayloadCompiler;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Http;
use Tests\TestCase;

class BridgePublisherTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config([
            'services.bridge.ingest_url' => 'https://bridge.local/api/internal/publish',
            'services.bridge.publish_secret' => 'pub-secret',
        ]);
    }

    public function test_push_sends_signed_payload_with_token_and_ping_url(): void
    {
        Http::fake(['*' => Http::response(['stored_version' => 3], 200)]);

        $site = Site::factory()->create(['domain' => 'domen.ua', 'ping_url' => 'https://domen.ua/ping']);
        $raw = app(SiteProvisioner::class)->issueToken($site);
        $publication = app(SitePayloadCompiler::class)->publish($site);

        $ok = app(BridgePublisher::class)->push($publication);

        $this->assertTrue($ok);
        Http::assertSent(function ($request) use ($site, $raw, $publication) {
            $body = $request->body();
            $expectedSig = hash_hmac('sha256', $body, 'pub-secret');
            $data = json_decode($body, true);

            return $request->url() === 'https://bridge.local/api/internal/publish'
                && $request->hasHeader('X-Signature', $expectedSig)
                && $data['domain'] === 'domen.ua'
                && $data['token_hash'] === hash('sha256', $raw)
                && $data['ping_url'] === 'https://domen.ua/ping'
                && $data['version'] === $publication->version
                && $data['payload']['site'] === 'domen.ua';
        });
    }

    public function test_push_returns_false_when_site_has_no_token(): void
    {
        Http::fake();
        $site = Site::factory()->create();
        $publication = app(SitePayloadCompiler::class)->publish($site);

        $ok = app(BridgePublisher::class)->push($publication);

        $this->assertFalse($ok);
        Http::assertNothingSent();
    }

    public function test_push_returns_false_on_bridge_error(): void
    {
        Http::fake(['*' => Http::response('', 500)]);
        $site = Site::factory()->create();
        app(SiteProvisioner::class)->issueToken($site);
        $publication = app(SitePayloadCompiler::class)->publish($site);

        $ok = app(BridgePublisher::class)->push($publication);

        $this->assertFalse($ok);
    }
}
```

`core/tests/Feature/WebhookPublishesToBridgeTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\Site;
use App\Services\Provisioning\SiteProvisioner;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Http;
use Tests\Support\BuildsSlots;
use Tests\TestCase;

class WebhookPublishesToBridgeTest extends TestCase
{
    use RefreshDatabase;
    use BuildsSlots;

    private const SECRET = 'mon-secret';

    protected function setUp(): void
    {
        parent::setUp();
        config([
            'services.monitoring.secret' => self::SECRET,
            'services.bridge.ingest_url' => 'https://bridge.local/api/internal/publish',
            'services.bridge.publish_secret' => 'pub-secret',
        ]);
    }

    public function test_webhook_failover_pushes_affected_site_to_bridge(): void
    {
        Http::fake(['*' => Http::response(['stored_version' => 1], 200)]);

        $site = Site::factory()->create(['domain' => 'domen.ua']);
        app(SiteProvisioner::class)->issueToken($site);
        [$slot, $entries] = $this->slotWithNumbers(['active', 'active']);
        $slot->dataValue->update([
            'key' => 'phone_ua_1', 'scope_type' => 'site', 'scope_id' => $site->id,
        ]);

        $body = json_encode(['e164' => $entries[0]->phoneNumber->e164, 'status' => 'down']);
        $this->call('POST', '/api/monitoring/numbers', [], [], [], [
            'CONTENT_TYPE' => 'application/json',
            'HTTP_X_SIGNATURE' => hash_hmac('sha256', $body, self::SECRET),
            'HTTP_ACCEPT' => 'application/json',
        ], $body)->assertOk();

        Http::assertSent(fn ($request) => $request->url() === 'https://bridge.local/api/internal/publish'
            && json_decode($request->body(), true)['domain'] === 'domen.ua');
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=BridgePublisherTest`
Expected: FAIL.

- [ ] **Step 3: Конфіг**

У `core/config/services.php` додати:
```php
'bridge' => [
    'ingest_url' => env('BRIDGE_INGEST_URL'),
    'publish_secret' => env('BRIDGE_PUBLISH_SECRET'),
],
```
У `core/.env.example` додати:
```dotenv
BRIDGE_INGEST_URL=
BRIDGE_PUBLISH_SECRET=
```

- [ ] **Step 4: Публікатор**

`core/app/Services/Publishing/BridgePublisher.php`:
```php
<?php

namespace App\Services\Publishing;

use App\Models\Publication;
use App\Models\Site;
use App\Services\Provisioning\SiteProvisioner;
use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Log;

class BridgePublisher
{
    public function __construct(private SiteProvisioner $provisioner) {}

    /** Надіслати останній payload сайта в DataBridge. true — прийнято, false — пропущено/помилка. */
    public function push(Publication $publication): bool
    {
        $site = Site::find($publication->site_id);
        if (! $site) {
            return false;
        }

        $tokenHash = $this->provisioner->activeTokenHash($site);
        if (! $tokenHash) {
            return false; // сайт без токена — публікувати нікуди
        }

        $url = (string) config('services.bridge.ingest_url');
        $secret = (string) config('services.bridge.publish_secret');
        if ($url === '' || $secret === '') {
            return false;
        }

        $body = json_encode([
            'domain' => $site->domain,
            'token_hash' => $tokenHash,
            'ping_url' => $site->ping_url,
            'version' => $publication->version,
            'payload' => $publication->payload,
        ], JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES);

        $signature = hash_hmac('sha256', $body, $secret);

        try {
            $response = Http::withHeaders([
                'Content-Type' => 'application/json',
                'X-Signature' => $signature,
            ])->withBody($body, 'application/json')->post($url);
        } catch (\Throwable $e) {
            Log::warning('Bridge push failed', ['site' => $site->domain, 'error' => $e->getMessage()]);

            return false;
        }

        return $response->successful();
    }
}
```

- [ ] **Step 5: Інтеграція у webhook (публікація в bridge ПІСЛЯ транзакції)**

У `core/app/Http/Controllers/Api/MonitoringWebhookController.php`:
- додати в сигнатуру `__invoke` параметр `BridgePublisher $publisher` (і `use App\Services\Publishing\BridgePublisher;`, `use App\Models\Publication;`).
- після того, як транзакція повернула `$sites` і ДО `return`, додати публікацію кожного ураженого сайта в bridge на основі його НАЙНОВІШОЇ публікації:
```php
$sites->each(function ($site) use ($publisher) {
    $publication = Publication::where('site_id', $site->id)->latest('version')->first();
    if ($publication) {
        $publisher->push($publication);
    }
});
```
Це поза транзакцією (мережеві виклики не тримають БД-локи). Невдала доставка в bridge не валить webhook — bridge добере дані наступним пушем або звіркою.

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm core php artisan test --filter=BridgePublisherTest`
Expected: PASS (3 tests).
Run: `docker compose run --rm core php artisan test --filter=WebhookPublishesToBridgeTest`
Expected: PASS (1 test).
Then: `docker compose run --rm core php artisan test`
Expected: 49 passed (45 + 3 + 1).

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager: публікація уражених сайтів у DataBridge після failover"
```

---

### Task 10: Контрактний round-trip (Core payload → Bridge → сайт)

**Files:**
- Test: `bridge/tests/Feature/ContractRoundTripTest.php`

Підтверджує: payload у форматі Core (як його шле BridgePublisher) приймається ingest'ом і повертається serve'ом без втрат. Це фіксує сумісність контракту між двома сервісами (вони деплояться окремо).

- [ ] **Step 1: Написати тест**

`bridge/tests/Feature/ContractRoundTripTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Jobs\DeliverPingJob;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Queue;
use Tests\TestCase;

class ContractRoundTripTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config([
            'services.publish.secret' => 'pub',
            'services.data.signing_secret' => 'sign',
        ]);
        Queue::fake();
    }

    public function test_core_shaped_payload_ingested_and_served_intact(): void
    {
        $rawToken = 'site-raw-token';

        // payload рівно у формі, що компілює Core (плани 1.1)
        $payload = [
            'site' => 'domen.ua',
            'version' => 2,
            'generated_at' => '2026-06-13T10:00:00+00:00',
            'values' => [
                ['key' => 'phone_ua_1', 'type' => 'phone', 'geo' => ['UA'], 'state' => 'ok', 'value' => '+380441234567'],
                ['key' => 'viber_ua_2', 'type' => 'messenger', 'geo' => ['UA'], 'network' => 'viber',
                 'state' => 'ok', 'value' => '+380671112233', 'url' => 'viber://chat?number=%2B380671112233'],
                ['key' => 'price_basic', 'type' => 'price', 'geo' => ['WORLD'], 'value' => '1200', 'currency' => 'UAH'],
            ],
        ];

        $ingestBody = json_encode([
            'domain' => 'domen.ua',
            'token_hash' => hash('sha256', $rawToken),
            'ping_url' => 'https://domen.ua/ping',
            'version' => 2,
            'payload' => $payload,
        ]);

        // 1. Core → Bridge ingest
        $this->call('POST', '/api/internal/publish', [], [], [], [
            'CONTENT_TYPE' => 'application/json',
            'HTTP_X_SIGNATURE' => hash_hmac('sha256', $ingestBody, 'pub'),
            'HTTP_ACCEPT' => 'application/json',
        ], $ingestBody)->assertOk();

        // 2. Сайт → Bridge serve
        $response = $this->getJson('/api/v1/data', ['X-Site-Token' => $rawToken])->assertOk();

        // 3. Контракт цілий
        $served = $response->json();
        $this->assertSame($payload, $served);
        $this->assertSame('+380671112233', $served['values'][1]['value']);

        // 4. Підпис відповіді валідний
        $body = $response->getContent();
        $response->assertHeader('X-Signature', hash_hmac('sha256', $body, 'sign'));
    }
}
```

- [ ] **Step 2: Прогнати — очікувано зелений**

Run: `docker compose run --rm bridge php artisan test --filter=ContractRoundTripTest`
Expected: PASS (1 test). Якщо `assertSame($payload, $served)` падає через екранування/типи — діагностуй serve-серіалізацію (DataController має повертати payload як збережено; JSON-прапори UNESCAPED). Виправляй контролер, не тест.
Then: `docker compose run --rm bridge php artisan test`
Expected: 22 passed.

- [ ] **Step 3: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): контрактний round-trip Core → Bridge → сайт"
```

---

### Task 11: Фінал — прогони, міграції MySQL, контекст, пуш

**Files:**
- Modify: `10-Projects/DBManager/DBManager — Контекст.md`
- Modify: `10-Projects/DBManager/DBManager — План.md`

- [ ] **Step 1: Повні прогони обох сервісів**

Run:
```bash
docker compose run --rm core php artisan test
docker compose run --rm bridge php artisan test
```
Expected: core 49 passed; bridge 22 passed; 0 failures.

- [ ] **Step 2: Міграції на живих БД**

Run:
```bash
docker compose up -d mysql bridge-mysql redis
docker compose run --rm core php artisan migrate --force
docker compose run --rm bridge php artisan migrate --force
```
Expected: core застосовує `add_ping_url_to_sites`; bridge створює свої таблиці. Обидва — без помилок.

- [ ] **Step 3: Оновити контекст і план**

У `DBManager — Контекст.md`: стан → «Плани 1.1 і 1.2 виконано: Core + DataBridge з доставкою»; наступний крок → «План 1.3 — WP-плагін DBManager (кеш, шорткоди, вкладки, mu-фолбек)». У `DBManager — План.md` (Етап 1) відмітити план 1.2 виконаним, лишити 1.3 наступним.

- [ ] **Step 4: Commit + push**

```bash
git add 10-Projects/DBManager
git commit -m "dbmanager: завершено план 1.2 — DataBridge і доставка"
git push
```

---

## Покриття спеки планом 1.2 (self-review)

- Дизайн §3 (DataBridge окремий, публічний, read-only копія, ізольований від БД Core) → Tasks 1–2 (власна БД bridge-mysql), серве не має доступу до Core.
- §5 (доставка): подієвий пуш Core→Bridge → Task 9; push-пінг сайтам із ретраями/backoff → Task 7; добова звірка версії (If-None-Match → 304) → Tasks 4–5. Постійного pull немає.
- §9 (безпека): HMAC на ingest (Task 3), підпис відповідей serve (Task 4), окремі секрети publish/data/ping (Task 1 env), rate limiting + журнал запитів (Task 6), токени read-only по сайту з хешуванням (Task 8). Ізоляція: bridge не торкається БД Core; токен у serve лише читає свій payload.
- Контракт payload незмінний від плану 1.1; round-trip перевіряє сумісність (Task 10).
- Не входить у 1.2 (свідомо): WP-плагін і mu-фолбек (план 1.3); реальний планувальник добової звірки на стороні сайта (це плагін, 1.3); адмінка/ролі/імпорт (Етап 2); черга воркера в проді й рознесення хостів (деплой). Throttle на serve — базовий 120/хв; тонке налаштування — деплой.
