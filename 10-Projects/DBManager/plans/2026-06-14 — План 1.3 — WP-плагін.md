# План імплементації 1.3 — WP-плагін DBManager (Client)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** WP-плагін «DBManager», що тягне опубліковані дані з DataBridge по токену (з перевіркою HMAC-підпису й ETag), кешує їх у `wp_options`, приймає push-пінги та робить добову звірку, рендерить значення з кешу через шорткод/функцію (нуль звернень до bridge на показі сторінки), і виживає через mu-plugin фолбек при видаленні основного плагіна.

**Architecture:** Уся бізнес-логіка — WP-незалежні чисті класи в `src/` (резолвер значень, рендерер, перевірка підпису, синхронізатор над портами Cache/HTTP) — тестується PHPUnit з in-memory фейками, без бутстрапу WordPress. Тонка WP-обв'язка (хуки, REST-маршрут пінга, WP-cron, 3 сторінки адмінки, `wp_options`-адаптер) лише підключає чисту логіку. Спільний чистий рендер (`render-core.php`) фізично копіюється і в основний плагін, і в `mu-plugins/`, тож mu-фолбек самодостатній і не залежить від файлів плагіна, який можна видалити.

**Tech Stack:** PHP 8.4, WordPress (плагін, без фреймворку), Composer + PHPUnit (юніт-логіка), WordPress + WP-CLI у Docker (інтеграційні сценарії), HMAC-SHA256 (перевірка підпису даних і пінгів). Нуль зовнішніх runtime-залежностей (пріоритет №1 дизайну).

**Спека:** `10-Projects/DBManager/DBManager — Дизайн.md` (§5 доставка, §8 плагін, §9 безпека, §10 відмови, §12 тестування). Контракт payload і доставки — від планів 1.1/1.2: `GET /v1/data` з `X-Site-Token`, відповідь із `ETag: "<version>"` і `X-Signature: hmac_sha256(body, data_signing_secret)`; пінг — `POST {ping_url}` з тілом `{"domain","version"}` і `X-Signature: hmac_sha256(body, ping_secret)`.

**Гео в скоупі 1.3 (рішення 2026-06-14, [[2026-06-14 — Гео-рендер плагіна на PHP без JS|ADR]]):** увесь рендер і гео-детект — суто PHP, **без жодного JS**. Гео = видимість: значення показується, якщо детектована країна ∈ `geo` або `geo` містить `WORLD`. Країна: заголовок `CF-IPCountry` → локальний MaxMind-лукап → `WORLD`. MaxMind-базу роздає центр через bridge (дзеркалить пайплайн даних); плагін кешує її локально й читає вбудованим чистим-PHP `maxmind-db/reader` (self-hosted, нуль зовнішніх runtime-викликів). Наслідок деплою: повносторінковий кеш має варіюватися за `CF-IPCountry` або гео-сторінки виключаються з кешу. **Поза 1.3:** власний сервер оновлень плагіна, блоки Gutenberg — пізніші етапи.

**Робоча директорія docker-команд:** `C:/Dev/Meweek/10-Projects/DBManager/code` (bash). Git — з кореня репо `C:/Dev/Meweek`, коміти українською `dbmanager(plugin): ...`, тільки шляхи під `10-Projects/DBManager/code/plugin`, без push (крім фінального таска).

**Узгодження секретів (критично):** плагін зберігає в налаштуваннях `bridge_url`, `site_token`, `data_signing_secret` (для перевірки `X-Signature` відповіді), `ping_secret` (для перевірки пінга). Перевірка підпису — завжди `hash_equals` над `hash_hmac('sha256', $rawBody, $secret)`; порівняння над сирим тілом до декодування JSON.

**Структура файлів (`code/plugin/`):**

```
code/plugin/
  composer.json                         # PSR-4 DBM\ → src/, dev: phpunit
  phpunit.xml                           # тести tests/Unit (без БД, без WP)
  shared/render-core.php                # ЄДИНА чиста функція рендеру (WP-free), джерело правди
  dbmanager/
    dbmanager.php                       # bootstrap основного плагіна (header + wiring)
    src/
      Sync/PayloadVerifier.php          # перевірка X-Signature
      Sync/Synchronizer.php             # fetch+verify+store+reconcile над портами
      Sync/SyncResult.php               # DTO результату
      Cache/CacheStore.php              # порт кешу
      Cache/WpOptionCacheStore.php      # адаптер wp_options
      Http/DataClient.php               # порт HTTP-клієнта bridge
      Http/WpHttpDataClient.php         # адаптер wp_remote_get
      Geo/GeoDetector.php               # країна: CF-IPCountry → лукап → WORLD
      Geo/CountryLookup.php             # порт лукапу IP→країна
      Geo/MaxMindCountryLookup.php      # адаптер чистого-PHP maxmind-db/reader
      Geo/GeoDbClient.php               # порт завантаження .mmdb з bridge
      Geo/GeoDbStore.php                # порт локального сховища .mmdb
      Geo/GeoDbSynchronizer.php         # завантаження+верифікація+кеш бази
      Config/Settings.php               # DTO налаштувань
      Wp/Plugin.php                     # реєстрація хуків/шорткода/cron/REST/admin
      Rest/PingController.php           # приймач /wp-json/dbm/v1/ping
      Admin/AdminPages.php              # 3 сторінки + перевірка з'єднання
      Admin/SnippetBuilder.php          # генерація сніпетів для вкладки «Вставка»
  mu/
    dbmanager-fallback.php              # mu-plugin фолбек (самодостатній)
  tests/
    Unit/                               # PHPUnit-тести чистої логіки
    Support/InMemoryCacheStore.php      # фейк кешу
    Support/FakeDataClient.php          # фейк HTTP-клієнта
  bin/install-wp.sh                     # інтеграційний сетап (WP-CLI)
```

---

### Task 1: Каркас плагіна + composer/phpunit + compose-сервіс тестів

**Files:**
- Modify: `10-Projects/DBManager/code/docker-compose.yml`
- Create: `code/plugin/composer.json`
- Create: `code/plugin/phpunit.xml`
- Create: `code/plugin/dbmanager/dbmanager.php`
- Create: `code/plugin/tests/Unit/SmokeTest.php`

- [ ] **Step 1: composer.json**

`code/plugin/composer.json`:
```json
{
    "name": "dbmanager/client",
    "description": "DBManager WordPress client plugin",
    "type": "wordpress-plugin",
    "require": { "php": ">=8.2" },
    "require-dev": { "phpunit/phpunit": "^11.0" },
    "autoload": { "psr-4": { "DBM\\": "dbmanager/src/" } },
    "autoload-dev": { "psr-4": { "DBM\\Tests\\": "tests/" } },
    "config": { "optimize-autoloader": true }
}
```

- [ ] **Step 2: phpunit.xml**

`code/plugin/phpunit.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
    </testsuites>
</phpunit>
```

- [ ] **Step 3: Bootstrap основного плагіна (поки лише header)**

`code/plugin/dbmanager/dbmanager.php`:
```php
<?php
/**
 * Plugin Name: DBManager
 * Description: Read-only клієнт центральних даних (DBManager Core / DataBridge).
 * Version: 0.1.0
 * Requires PHP: 8.2
 */

if (! defined('ABSPATH')) {
    return; // прямий виклик поза WordPress — нічого не робимо
}

require_once __DIR__ . '/../shared/render-core.php';

spl_autoload_register(function (string $class): void {
    if (! str_starts_with($class, 'DBM\\')) {
        return;
    }
    $path = __DIR__ . '/src/' . str_replace('\\', '/', substr($class, 4)) . '.php';
    if (is_file($path)) {
        require_once $path;
    }
});

(new DBM\Wp\Plugin())->register();
```

- [ ] **Step 4: Смоук-тест автозавантаження**

`code/plugin/tests/Unit/SmokeTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use PHPUnit\Framework\TestCase;

class SmokeTest extends TestCase
{
    public function test_autoloader_is_wired(): void
    {
        $this->assertTrue(class_exists(\DBM\Tests\Unit\SmokeTest::class));
    }
}
```

- [ ] **Step 5: compose-сервіс `plugin` (раннер юніт-тестів)**

У `docker-compose.yml` додати сервіс (образ той самий узагальнений PHP-CLI, що в core/bridge):
```yaml
  plugin:
    build: ./docker/core
    volumes:
      - ./plugin:/app
    working_dir: /app
```

- [ ] **Step 6: Встановити залежності і прогнати**

Run (з `code/`):
```bash
docker compose run --rm plugin composer install
docker compose run --rm plugin ./vendor/bin/phpunit
```
Expected: `OK (1 test)`.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/plugin 10-Projects/DBManager/code/docker-compose.yml
git commit -m "dbmanager(plugin): каркас WP-плагіна + раннер юніт-тестів"
```

---

### Task 2: render-core — пошук значення + базовий вивід (span)

**Files:**
- Create: `code/plugin/shared/render-core.php`
- Test: `code/plugin/tests/Unit/RenderCoreTest.php`

`dbm_render_from_cache()` — ЄДИНА чиста (WP-free) функція рендеру, самодостатня (без класів-залежностей), тож її можна скопіювати в `mu-plugins/` поряд із фолбеком. Її використовують і основний плагін, і mu-фолбек. У цьому таску — пошук значення + базовий `<span>`; формати tel/link — Task 3.

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/RenderCoreTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use PHPUnit\Framework\TestCase;

class RenderCoreTest extends TestCase
{
    protected function setUp(): void
    {
        require_once __DIR__ . '/../../shared/render-core.php';
    }

    private function payload(): array
    {
        return ['version' => 4, 'values' => [
            ['key' => 'phone_ua_1', 'type' => 'phone', 'state' => 'ok', 'value' => '+380441234567'],
            ['key' => 'gone', 'type' => 'phone', 'state' => 'exhausted', 'value' => ''],
        ]];
    }

    public function test_renders_plain_value_in_neutral_span(): void
    {
        $this->assertSame('<span>+380441234567</span>',
            dbm_render_from_cache($this->payload(), 'phone_ua_1', []));
    }

    public function test_applies_configured_css_class(): void
    {
        $this->assertSame('<span class="val">+380441234567</span>',
            dbm_render_from_cache($this->payload(), 'phone_ua_1', ['class' => 'val']));
    }

    public function test_exhausted_state_renders_nothing(): void
    {
        $this->assertSame('', dbm_render_from_cache($this->payload(), 'gone', []));
    }

    public function test_unknown_key_renders_nothing(): void
    {
        $this->assertSame('', dbm_render_from_cache($this->payload(), 'nope', []));
    }

    public function test_value_is_html_escaped(): void
    {
        $payload = ['values' => [['key' => 'x', 'state' => 'ok', 'value' => '<b>&"']]];
        $this->assertStringNotContainsString('<b>', dbm_render_from_cache($payload, 'x', []));
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter RenderCoreTest`
Expected: FAIL (function not defined).

- [ ] **Step 3: Реалізація (мінімальна — span)**

`code/plugin/shared/render-core.php`:
```php
<?php

// Спільне ядро рендеру: одне джерело правди для основного плагіна і mu-фолбека.
// Без жодних викликів WP — лише чисте перетворення кеш-payload у рядок.

if (! function_exists('dbm_render_from_cache')) {
    /**
     * @param array $payload розпакований кеш (site/version/values)
     * @param array $opts    ['format' => 'tel'|'link'|'', 'class' => string]
     */
    function dbm_render_from_cache(array $payload, string $key, array $opts = []): string
    {
        $value = null;
        foreach ($payload['values'] ?? [] as $candidate) {
            if (($candidate['key'] ?? null) === $key) {
                $value = $candidate;
                break;
            }
        }
        if ($value === null || in_array($value['state'] ?? 'ok', ['hidden', 'exhausted'], true)) {
            return '';
        }
        $raw = (string) ($value['value'] ?? '');
        if ($raw === '') {
            return '';
        }

        $cls = ($opts['class'] ?? '') !== ''
            ? ' class="' . htmlspecialchars((string) $opts['class'], ENT_QUOTES) . '"'
            : '';

        return '<span' . $cls . '>' . htmlspecialchars($raw, ENT_QUOTES) . '</span>';
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter RenderCoreTest`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): спільне ядро рендеру з кешу (span, анти-fingerprint)"
```

---

### Task 3: render-core — формати tel і link

**Files:**
- Modify: `code/plugin/shared/render-core.php`
- Test: `code/plugin/tests/Unit/RenderCoreFormatTest.php`

Розширюємо ту саму функцію: `format=tel` → `tel:`-лінк (тільки `+` і цифри в href); месенджер із полем `url` + `format=link` → лінк на `url`. Анти-fingerprint: ті самі нейтральні класи, без data-атрибутів.

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/RenderCoreFormatTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use PHPUnit\Framework\TestCase;

class RenderCoreFormatTest extends TestCase
{
    protected function setUp(): void
    {
        require_once __DIR__ . '/../../shared/render-core.php';
    }

    private function payload(): array
    {
        return ['values' => [
            ['key' => 'phone_ua_1', 'type' => 'phone', 'state' => 'ok', 'value' => '+380441234567'],
            ['key' => 'vb', 'type' => 'messenger', 'state' => 'ok', 'value' => '+380671112233',
             'url' => 'viber://chat?number=%2B380671112233'],
        ]];
    }

    public function test_tel_format_wraps_in_tel_link(): void
    {
        $this->assertSame(
            '<a class="val" href="tel:+380441234567">+380441234567</a>',
            dbm_render_from_cache($this->payload(), 'phone_ua_1', ['format' => 'tel', 'class' => 'val'])
        );
    }

    public function test_messenger_link_uses_url(): void
    {
        $html = dbm_render_from_cache($this->payload(), 'vb', ['format' => 'link']);
        $this->assertStringContainsString('href="viber://chat?number=%2B380671112233"', $html);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter RenderCoreFormatTest`
Expected: FAIL (tel/link ще не реалізовано — повертається span).

- [ ] **Step 3: Розширити функцію (додати гілки форматів перед `return '<span...'`)**

У `code/plugin/shared/render-core.php`, всередині `dbm_render_from_cache`, замінити фінальний `return '<span...'` на:
```php
        $text = htmlspecialchars($raw, ENT_QUOTES);
        $format = $opts['format'] ?? '';

        if ($format === 'tel') {
            $href = htmlspecialchars('tel:' . preg_replace('/[^+\d]/', '', $raw), ENT_QUOTES);

            return '<a' . $cls . ' href="' . $href . '">' . $text . '</a>';
        }
        if ($format === 'link' && ! empty($value['url'])) {
            return '<a' . $cls . ' href="' . htmlspecialchars((string) $value['url'], ENT_QUOTES) . '">' . $text . '</a>';
        }

        return '<span' . $cls . '>' . $text . '</span>';
```
(рядок `$cls = ...` із Task 2 лишається вище без змін).

- [ ] **Step 4: Прогнати — зелений (нові + старі)**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter "RenderCore"`
Expected: PASS (RenderCoreTest 5 + RenderCoreFormatTest 2).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): формати tel і link у ядрі рендеру"
```

---

### Task 4: PayloadVerifier — перевірка HMAC-підпису

**Files:**
- Create: `code/plugin/dbmanager/src/Sync/PayloadVerifier.php`
- Test: `code/plugin/tests/Unit/PayloadVerifierTest.php`

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/PayloadVerifierTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Sync\PayloadVerifier;
use PHPUnit\Framework\TestCase;

class PayloadVerifierTest extends TestCase
{
    public function test_accepts_correct_signature(): void
    {
        $body = '{"site":"domen.ua","version":4}';
        $sig = hash_hmac('sha256', $body, 'secret');

        $this->assertTrue((new PayloadVerifier())->verify($body, $sig, 'secret'));
    }

    public function test_rejects_wrong_signature(): void
    {
        $this->assertFalse((new PayloadVerifier())->verify('{"a":1}', 'deadbeef', 'secret'));
    }

    public function test_rejects_empty_secret(): void
    {
        $body = '{"a":1}';
        $sig = hash_hmac('sha256', $body, '');

        // Порожній секрет — fail-closed навіть за «правильного» підпису порожнім ключем.
        $this->assertFalse((new PayloadVerifier())->verify($body, $sig, ''));
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter PayloadVerifierTest`
Expected: FAIL.

- [ ] **Step 3: Реалізація**

`code/plugin/dbmanager/src/Sync/PayloadVerifier.php`:
```php
<?php

namespace DBM\Sync;

class PayloadVerifier
{
    /** Перевірка підпису над СИРИМ тілом. Порожній секрет — завжди false (fail-closed). */
    public function verify(string $rawBody, string $signature, string $secret): bool
    {
        if ($secret === '') {
            return false;
        }

        return hash_equals(hash_hmac('sha256', $rawBody, $secret), $signature);
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter PayloadVerifierTest`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): перевірка HMAC-підпису (fail-closed)"
```

---

### Task 5: Порти Cache/HTTP + фейки + адаптери

**Files:**
- Create: `code/plugin/dbmanager/src/Cache/CacheStore.php`
- Create: `code/plugin/dbmanager/src/Http/DataClient.php`
- Create: `code/plugin/dbmanager/src/Cache/WpOptionCacheStore.php`
- Create: `code/plugin/dbmanager/src/Http/WpHttpDataClient.php`
- Create: `code/plugin/tests/Support/InMemoryCacheStore.php`
- Create: `code/plugin/tests/Support/FakeDataClient.php`
- Test: `code/plugin/tests/Unit/InMemoryCacheStoreTest.php`

- [ ] **Step 1: Написати тест (контракт порту через фейк)**

`code/plugin/tests/Unit/InMemoryCacheStoreTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Tests\Support\InMemoryCacheStore;
use PHPUnit\Framework\TestCase;

class InMemoryCacheStoreTest extends TestCase
{
    public function test_stores_and_reads_payload_and_version(): void
    {
        $store = new InMemoryCacheStore();
        $store->put(['site' => 'd.ua', 'version' => 5, 'values' => []]);

        $this->assertSame(5, $store->version());
        $this->assertSame('d.ua', $store->get()['site']);
    }

    public function test_version_is_zero_when_empty(): void
    {
        $this->assertSame(0, (new InMemoryCacheStore())->version());
        $this->assertNull((new InMemoryCacheStore())->get());
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter InMemoryCacheStoreTest`
Expected: FAIL.

- [ ] **Step 3: Порти й фейки**

`code/plugin/dbmanager/src/Cache/CacheStore.php`:
```php
<?php

namespace DBM\Cache;

interface CacheStore
{
    public function get(): ?array;       // розпакований payload або null

    public function version(): int;      // версія в кеші (0 якщо порожній)

    public function put(array $payload): void;

    public function clear(): void;       // лише за явним запитом
}
```

`code/plugin/dbmanager/src/Http/DataClient.php`:
```php
<?php

namespace DBM\Http;

interface DataClient
{
    /**
     * GET даних bridge.
     * @return array{status:int, body:string, signature:string, etag:string}
     */
    public function fetch(string $url, string $token, ?int $ifNoneMatchVersion): array;
}
```

`code/plugin/tests/Support/InMemoryCacheStore.php`:
```php
<?php

namespace DBM\Tests\Support;

use DBM\Cache\CacheStore;

class InMemoryCacheStore implements CacheStore
{
    private ?array $payload = null;

    public function get(): ?array
    {
        return $this->payload;
    }

    public function version(): int
    {
        return (int) ($this->payload['version'] ?? 0);
    }

    public function put(array $payload): void
    {
        $this->payload = $payload;
    }

    public function clear(): void
    {
        $this->payload = null;
    }
}
```

`code/plugin/tests/Support/FakeDataClient.php`:
```php
<?php

namespace DBM\Tests\Support;

use DBM\Http\DataClient;

class FakeDataClient implements DataClient
{
    /** @var array<int, array> */
    public array $calls = [];

    public function __construct(private array $response) {}

    public function fetch(string $url, string $token, ?int $ifNoneMatchVersion): array
    {
        $this->calls[] = compact('url', 'token', 'ifNoneMatchVersion');

        return $this->response;
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter InMemoryCacheStoreTest`
Expected: PASS (2 tests).

- [ ] **Step 5: WP-адаптери (тонкі, без юніт-тестів — покриються інтеграційно у Task 11)**

`code/plugin/dbmanager/src/Cache/WpOptionCacheStore.php`:
```php
<?php

namespace DBM\Cache;

class WpOptionCacheStore implements CacheStore
{
    private const OPTION = 'dbm_cache';

    public function get(): ?array
    {
        $stored = get_option(self::OPTION);

        return is_array($stored) ? $stored : null;
    }

    public function version(): int
    {
        return (int) ($this->get()['version'] ?? 0);
    }

    public function put(array $payload): void
    {
        // autoload=true: доступний на фронті без зайвого запиту; не чиститься при деактивації.
        update_option(self::OPTION, $payload, true);
    }

    public function clear(): void
    {
        delete_option(self::OPTION);
    }
}
```

`code/plugin/dbmanager/src/Http/WpHttpDataClient.php`:
```php
<?php

namespace DBM\Http;

class WpHttpDataClient implements DataClient
{
    public function fetch(string $url, string $token, ?int $ifNoneMatchVersion): array
    {
        $headers = ['X-Site-Token' => $token];
        if ($ifNoneMatchVersion !== null) {
            $headers['If-None-Match'] = '"' . $ifNoneMatchVersion . '"';
        }

        $response = wp_remote_get($url, ['headers' => $headers, 'timeout' => 8]);

        if (is_wp_error($response)) {
            return ['status' => 0, 'body' => '', 'signature' => '', 'etag' => ''];
        }

        return [
            'status' => (int) wp_remote_retrieve_response_code($response),
            'body' => (string) wp_remote_retrieve_body($response),
            'signature' => (string) wp_remote_retrieve_header($response, 'x-signature'),
            'etag' => (string) wp_remote_retrieve_header($response, 'etag'),
        ];
    }
}
```

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): порти Cache/HTTP, фейки і wp_options/wp_remote_get адаптери"
```

---

### Task 6: Synchronizer — fetch+verify+store+звірка

**Files:**
- Create: `code/plugin/dbmanager/src/Sync/SyncResult.php`
- Create: `code/plugin/dbmanager/src/Sync/Synchronizer.php`
- Test: `code/plugin/tests/Unit/SynchronizerTest.php`

Поведінка (§5, §10): `sync()` — повний тяг (без ETag) на пінг; `reconcile()` — звірка з `If-None-Match`. 200 з валідним підписом → зберегти; 304 → лишити кеш; битий підпис або не-2xx/мережа → лишити кеш (сайт безстроково з кешу).

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/SynchronizerTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Sync\PayloadVerifier;
use DBM\Sync\Synchronizer;
use DBM\Tests\Support\FakeDataClient;
use DBM\Tests\Support\InMemoryCacheStore;
use PHPUnit\Framework\TestCase;

class SynchronizerTest extends TestCase
{
    private const SIGN = 'sign-secret';

    private function payloadBody(int $version): string
    {
        return json_encode(['site' => 'd.ua', 'version' => $version, 'values' => []]);
    }

    private function ok(int $version): array
    {
        $body = $this->payloadBody($version);

        return ['status' => 200, 'body' => $body, 'etag' => '"' . $version . '"',
                'signature' => hash_hmac('sha256', $body, self::SIGN)];
    }

    private function sync(FakeDataClient $client, InMemoryCacheStore $cache): Synchronizer
    {
        return new Synchronizer($client, $cache, new PayloadVerifier(),
            url: 'https://bridge/api/v1/data', token: 'tok', signingSecret: self::SIGN);
    }

    public function test_sync_stores_verified_payload(): void
    {
        $cache = new InMemoryCacheStore();
        $result = $this->sync(new FakeDataClient($this->ok(7)), $cache)->sync();

        $this->assertTrue($result->updated);
        $this->assertSame(7, $cache->version());
    }

    public function test_sync_rejects_bad_signature_and_keeps_cache(): void
    {
        $cache = new InMemoryCacheStore();
        $cache->put(['site' => 'd.ua', 'version' => 3, 'values' => []]);
        $bad = $this->ok(9);
        $bad['signature'] = 'tampered';

        $result = $this->sync(new FakeDataClient($bad), $cache)->sync();

        $this->assertFalse($result->updated);
        $this->assertSame(3, $cache->version()); // кеш недоторканий
    }

    public function test_reconcile_sends_if_none_match_and_keeps_cache_on_304(): void
    {
        $cache = new InMemoryCacheStore();
        $cache->put(['site' => 'd.ua', 'version' => 5, 'values' => []]);
        $client = new FakeDataClient(['status' => 304, 'body' => '', 'etag' => '"5"', 'signature' => '']);

        $result = $this->sync($client, $cache)->reconcile();

        $this->assertFalse($result->updated);
        $this->assertSame(5, $client->calls[0]['ifNoneMatchVersion']);
        $this->assertSame(5, $cache->version());
    }

    public function test_network_failure_keeps_cache(): void
    {
        $cache = new InMemoryCacheStore();
        $cache->put(['site' => 'd.ua', 'version' => 2, 'values' => []]);
        $client = new FakeDataClient(['status' => 0, 'body' => '', 'etag' => '', 'signature' => '']);

        $result = $this->sync($client, $cache)->reconcile();

        $this->assertFalse($result->updated);
        $this->assertSame(2, $cache->version());
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter SynchronizerTest`
Expected: FAIL.

- [ ] **Step 3: Реалізація**

`code/plugin/dbmanager/src/Sync/SyncResult.php`:
```php
<?php

namespace DBM\Sync;

class SyncResult
{
    public function __construct(
        public bool $updated,
        public int $version,
        public string $reason = '',
    ) {}
}
```

`code/plugin/dbmanager/src/Sync/Synchronizer.php`:
```php
<?php

namespace DBM\Sync;

use DBM\Cache\CacheStore;
use DBM\Http\DataClient;

class Synchronizer
{
    public function __construct(
        private DataClient $client,
        private CacheStore $cache,
        private PayloadVerifier $verifier,
        private string $url,
        private string $token,
        private string $signingSecret,
    ) {}

    /** Повний тяг (на пінг / ручну перевірку): без If-None-Match. */
    public function sync(): SyncResult
    {
        return $this->pull(null);
    }

    /** Добова звірка: з If-None-Match поточної версії. */
    public function reconcile(): SyncResult
    {
        return $this->pull($this->cache->version() ?: null);
    }

    private function pull(?int $ifNoneMatch): SyncResult
    {
        $current = $this->cache->version();
        $response = $this->client->fetch($this->url, $this->token, $ifNoneMatch);

        if ($response['status'] === 304) {
            return new SyncResult(false, $current, 'not-modified');
        }
        if ($response['status'] !== 200) {
            return new SyncResult(false, $current, 'unreachable'); // кеш лишається
        }
        if (! $this->verifier->verify($response['body'], $response['signature'], $this->signingSecret)) {
            return new SyncResult(false, $current, 'bad-signature'); // кеш лишається
        }

        $payload = json_decode($response['body'], true);
        if (! is_array($payload) || ! isset($payload['version'])) {
            return new SyncResult(false, $current, 'malformed');
        }

        $this->cache->put($payload);

        return new SyncResult(true, (int) $payload['version'], 'updated');
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter SynchronizerTest`
Expected: PASS (4 tests).

- [ ] **Step 5: Повний прогін**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit`
Expected: усі зелені.

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): синхронізатор (тяг/звірка, fail-soft на кеш)"
```

---

### Task 7: Settings + SnippetBuilder

**Files:**
- Create: `code/plugin/dbmanager/src/Config/Settings.php`
- Create: `code/plugin/dbmanager/src/Admin/SnippetBuilder.php`
- Test: `code/plugin/tests/Unit/SettingsTest.php`
- Test: `code/plugin/tests/Unit/SnippetBuilderTest.php`

- [ ] **Step 1: Написати тести**

`code/plugin/tests/Unit/SettingsTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Config\Settings;
use PHPUnit\Framework\TestCase;

class SettingsTest extends TestCase
{
    public function test_from_array_has_neutral_defaults(): void
    {
        $s = Settings::fromArray([]);
        $this->assertSame('dbm', $s->shortcode);
        $this->assertSame('', $s->cssClass);
        $this->assertSame('', $s->bridgeUrl);
    }

    public function test_from_array_reads_values(): void
    {
        $s = Settings::fromArray(['bridge_url' => 'https://b', 'site_token' => 't',
            'shortcode' => 'phone', 'css_class' => 'c']);
        $this->assertSame('https://b', $s->bridgeUrl);
        $this->assertSame('phone', $s->shortcode);
    }
}
```

`code/plugin/tests/Unit/SnippetBuilderTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Admin\SnippetBuilder;
use PHPUnit\Framework\TestCase;

class SnippetBuilderTest extends TestCase
{
    public function test_builds_shortcode_and_php_snippets(): void
    {
        $b = new SnippetBuilder('dbm');
        $snips = $b->forKey('phone_ua_1');

        $this->assertSame('[dbm key="phone_ua_1"]', $snips['shortcode']);
        $this->assertSame('[dbm key="phone_ua_1" format="tel"]', $snips['tel']);
        $this->assertSame("dbm_get('phone_ua_1')", $snips['php']);
    }

    public function test_respects_custom_shortcode_name(): void
    {
        $this->assertSame('[phone key="x"]', (new SnippetBuilder('phone'))->forKey('x')['shortcode']);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter "SettingsTest|SnippetBuilderTest"`
Expected: FAIL.

- [ ] **Step 3: Реалізація**

`code/plugin/dbmanager/src/Config/Settings.php`:
```php
<?php

namespace DBM\Config;

class Settings
{
    public function __construct(
        public string $bridgeUrl,
        public string $siteToken,
        public string $signingSecret,
        public string $pingSecret,
        public string $shortcode,
        public string $cssClass,
    ) {}

    public static function fromArray(array $a): self
    {
        return new self(
            bridgeUrl: (string) ($a['bridge_url'] ?? ''),
            siteToken: (string) ($a['site_token'] ?? ''),
            signingSecret: (string) ($a['signing_secret'] ?? ''),
            pingSecret: (string) ($a['ping_secret'] ?? ''),
            shortcode: (string) ($a['shortcode'] ?? '') ?: 'dbm',
            cssClass: (string) ($a['css_class'] ?? ''),
        );
    }
}
```

`code/plugin/dbmanager/src/Admin/SnippetBuilder.php`:
```php
<?php

namespace DBM\Admin;

class SnippetBuilder
{
    public function __construct(private string $shortcode) {}

    /** @return array{shortcode:string, tel:string, php:string} */
    public function forKey(string $key): array
    {
        return [
            'shortcode' => '[' . $this->shortcode . ' key="' . $key . '"]',
            'tel' => '[' . $this->shortcode . ' key="' . $key . '" format="tel"]',
            'php' => "dbm_get('" . $key . "')",
        ];
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter "SettingsTest|SnippetBuilderTest"`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): налаштування і генератор сніпетів для вкладки «Вставка»"
```

---

### Task 8: PingController — приймач /wp-json/dbm/v1/ping

**Files:**
- Create: `code/plugin/dbmanager/src/Rest/PingController.php`
- Test: `code/plugin/tests/Unit/PingControllerTest.php`

Контролер чистий: приймає сире тіло + підпис + замикання-синхронізатор; перевіряє підпис `ping_secret`; на успіх кличе синхронізатор. WP-REST-обгортка — у Task 10.

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/PingControllerTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Rest\PingController;
use DBM\Sync\PayloadVerifier;
use PHPUnit\Framework\TestCase;

class PingControllerTest extends TestCase
{
    public function test_valid_ping_triggers_sync(): void
    {
        $body = json_encode(['domain' => 'd.ua', 'version' => 6]);
        $sig = hash_hmac('sha256', $body, 'ping-secret');
        $synced = false;

        $controller = new PingController(new PayloadVerifier(), 'ping-secret', function () use (&$synced) {
            $synced = true;
        });
        $status = $controller->handle($body, $sig);

        $this->assertSame(202, $status);
        $this->assertTrue($synced);
    }

    public function test_invalid_signature_is_rejected_without_sync(): void
    {
        $synced = false;
        $controller = new PingController(new PayloadVerifier(), 'ping-secret', function () use (&$synced) {
            $synced = true;
        });

        $this->assertSame(401, $controller->handle('{"domain":"d.ua","version":6}', 'wrong'));
        $this->assertFalse($synced);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter PingControllerTest`
Expected: FAIL.

- [ ] **Step 3: Реалізація**

`code/plugin/dbmanager/src/Rest/PingController.php`:
```php
<?php

namespace DBM\Rest;

use DBM\Sync\PayloadVerifier;

class PingController
{
    /** @param callable $onValid викликається при валідному пінгу (запускає синхронізацію) */
    public function __construct(
        private PayloadVerifier $verifier,
        private string $pingSecret,
        private $onValid,
    ) {}

    /** Повертає HTTP-статус: 202 прийнято, 401 битий підпис. */
    public function handle(string $rawBody, string $signature): int
    {
        if (! $this->verifier->verify($rawBody, $signature, $this->pingSecret)) {
            return 401;
        }

        ($this->onValid)();

        return 202;
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter PingControllerTest`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): контролер приймання push-пінга (HMAC)"
```

---

### Task 9: mu-plugin фолбек (самодостатній)

**Files:**
- Create: `code/plugin/mu/dbmanager-fallback.php`
- Test: `code/plugin/tests/Unit/MuFallbackRenderTest.php`

mu-фолбек реєструє шорткод/`dbm_get` із кешу `wp_options`, ТІЛЬКИ якщо основний плагін неактивний (інакше — мовчить, щоб не дублювати). Самодостатній: підключає власну копію `render-core.php` (її кладе install у `mu-plugins/` — Task 11). Тест перевіряє чисту функцію рендеру з кешу — спільне ядро однакове в обох.

- [ ] **Step 1: Написати тест (через спільне ядро render-core)**

`code/plugin/tests/Unit/MuFallbackRenderTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use PHPUnit\Framework\TestCase;

class MuFallbackRenderTest extends TestCase
{
    protected function setUp(): void
    {
        require_once __DIR__ . '/../../shared/render-core.php';
    }

    public function test_renders_from_cache_payload(): void
    {
        $cache = ['version' => 4, 'values' => [
            ['key' => 'phone_ua_1', 'state' => 'ok', 'value' => '+380441234567'],
        ]];

        $this->assertSame(
            '<span>+380441234567</span>',
            dbm_render_from_cache($cache, 'phone_ua_1', [])
        );
    }

    public function test_missing_key_renders_empty(): void
    {
        $this->assertSame('', dbm_render_from_cache(['values' => []], 'x', []));
    }
}
```

- [ ] **Step 2: Запустити — зелений (ядро з Task 3 уже є)**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter MuFallbackRenderTest`
Expected: PASS (2 tests). Якщо червоне — діагностуй `shared/render-core.php`, не тест.

- [ ] **Step 3: mu-plugin файл**

`code/plugin/mu/dbmanager-fallback.php`:
```php
<?php
/**
 * Plugin Name: DBManager Fallback
 * Description: Аварійний рендер значень із кешу, якщо основний плагін вимкнено/видалено.
 */

if (! defined('ABSPATH')) {
    return;
}

// Самодостатнє ядро рендеру (копія render-core.php кладеться поряд установником).
require_once __DIR__ . '/render-core.php';

add_action('init', function (): void {
    // Якщо основний плагін активний — він сам реєструє шорткод; mu мовчить.
    if (function_exists('dbm_get')) {
        return;
    }

    $cache = get_option('dbm_cache');
    $cache = is_array($cache) ? $cache : ['values' => []];
    $opts = get_option('dbm_settings');
    $shortcode = is_array($opts) && ! empty($opts['shortcode']) ? $opts['shortcode'] : 'dbm';
    $class = is_array($opts) ? (string) ($opts['css_class'] ?? '') : '';

    function dbm_get(string $key, array $opts = []): string
    {
        $cache = get_option('dbm_cache');
        $cache = is_array($cache) ? $cache : ['values' => []];
        $settings = get_option('dbm_settings');
        $opts['class'] = $opts['class'] ?? (is_array($settings) ? (string) ($settings['css_class'] ?? '') : '');

        return dbm_render_from_cache($cache, $key, $opts);
    }

    add_shortcode($shortcode, function ($atts) {
        $atts = shortcode_atts(['key' => '', 'format' => ''], $atts);

        return dbm_get((string) $atts['key'], ['format' => (string) $atts['format']]);
    });
});
```

- [ ] **Step 4: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): mu-plugin фолбек-рендерер із кешу"
```

---

### Task 10: WP-обв'язка — Plugin (шорткод, REST, cron, активація) + AdminPages

**Files:**
- Create: `code/plugin/dbmanager/src/Wp/Plugin.php`
- Create: `code/plugin/dbmanager/src/Admin/AdminPages.php`

Тонка обв'язка над уже протестованою логікою. Юніт-тестів немає (WP-залежне) — перевіряється інтеграційно (Task 11). Дотримати анти-fingerprint: жодних generator-тегів/коментарів/data-атрибутів у публічному HTML.

- [ ] **Step 1: Plugin (реєстрація хуків)**

`code/plugin/dbmanager/src/Wp/Plugin.php`:
```php
<?php

namespace DBM\Wp;

use DBM\Admin\AdminPages;
use DBM\Cache\WpOptionCacheStore;
use DBM\Config\Settings;
use DBM\Http\WpHttpDataClient;
use DBM\Rest\PingController;
use DBM\Sync\PayloadVerifier;
use DBM\Sync\Synchronizer;

class Plugin
{
    private function settings(): Settings
    {
        $opts = get_option('dbm_settings');

        return Settings::fromArray(is_array($opts) ? $opts : []);
    }

    private function synchronizer(): Synchronizer
    {
        $s = $this->settings();

        return new Synchronizer(
            new WpHttpDataClient(),
            new WpOptionCacheStore(),
            new PayloadVerifier(),
            rtrim($s->bridgeUrl, '/') . '/api/v1/data',
            $s->siteToken,
            $s->signingSecret,
        );
    }

    public function register(): void
    {
        add_action('init', [$this, 'registerShortcode']);
        add_action('rest_api_init', [$this, 'registerRest']);
        add_action('dbm_daily_reconcile', [$this, 'runReconcile']);
        add_filter('cron_schedules', fn ($s) => $s); // добова — через wp_schedule_event(daily)
        register_activation_hook(
            dirname(__DIR__, 2) . '/dbmanager.php',
            fn () => wp_next_scheduled('dbm_daily_reconcile') ?: wp_schedule_event(time() + 3600, 'daily', 'dbm_daily_reconcile')
        );
        register_deactivation_hook(
            dirname(__DIR__, 2) . '/dbmanager.php',
            fn () => wp_clear_scheduled_hook('dbm_daily_reconcile')
            // Кеш (dbm_cache) НЕ видаляємо — живучість даних (§8).
        );

        if (is_admin()) {
            (new AdminPages($this->settings()))->register();
        }
    }

    public function registerShortcode(): void
    {
        $s = $this->settings();

        if (! function_exists('dbm_get')) {
            require_once dirname(__DIR__, 2) . '/../shared/render-core.php';

            // Глобальна функція виводу з кешу.
            eval('function dbm_get(string $key, array $opts = []): string {
                $cache = get_option("dbm_cache"); $cache = is_array($cache) ? $cache : ["values" => []];
                $st = get_option("dbm_settings");
                $opts["class"] = $opts["class"] ?? (is_array($st) ? (string)($st["css_class"] ?? "") : "");
                return dbm_render_from_cache($cache, $key, $opts);
            }');
        }

        add_shortcode($s->shortcode, function ($atts) {
            $atts = shortcode_atts(['key' => '', 'format' => ''], $atts);

            return dbm_get((string) $atts['key'], ['format' => (string) $atts['format']]);
        });
    }

    public function registerRest(): void
    {
        $s = $this->settings();
        $sync = fn () => $this->synchronizer()->sync();
        $controller = new PingController(new PayloadVerifier(), $s->pingSecret, $sync);

        register_rest_route('dbm/v1', '/ping', [
            'methods' => 'POST',
            'permission_callback' => '__return_true',
            'callback' => function ($request) use ($controller) {
                $status = $controller->handle(
                    (string) $request->get_body(),
                    (string) $request->get_header('x-signature')
                );

                return new \WP_REST_Response(null, $status);
            },
        ]);
    }

    public function runReconcile(): void
    {
        $this->synchronizer()->reconcile();
    }
}
```

> Примітка для виконавця: `eval` для `dbm_get` — навмисний компроміс, щоб і основний плагін, і mu-фолбек оголошували ту саму глобальну функцію без колізій (guard `function_exists`). Якщо при рев'ю визнають небажаним — винести `dbm_get` у окремий підключений файл-функцію з guard замість `eval`.

- [ ] **Step 2: AdminPages (3 вкладки + перевірка з'єднання)**

`code/plugin/dbmanager/src/Admin/AdminPages.php`:
```php
<?php

namespace DBM\Admin;

use DBM\Config\Settings;

class AdminPages
{
    public function __construct(private Settings $settings) {}

    public function register(): void
    {
        add_action('admin_menu', function (): void {
            add_menu_page('DBManager', 'DBManager', 'edit_posts', 'dbm-data', [$this, 'renderData']);
            add_submenu_page('dbm-data', 'Дані', 'Дані', 'edit_posts', 'dbm-data', [$this, 'renderData']);
            add_submenu_page('dbm-data', 'Вставка', 'Вставка', 'edit_posts', 'dbm-insert', [$this, 'renderInsert']);
            add_submenu_page('dbm-data', 'Налаштування', 'Налаштування', 'manage_options', 'dbm-settings', [$this, 'renderSettings']);
        });
        add_action('admin_init', function (): void {
            register_setting('dbm', 'dbm_settings');
        });
    }

    public function renderData(): void
    {
        $cache = get_option('dbm_cache');
        $cache = is_array($cache) ? $cache : ['values' => []];
        echo '<div class="wrap"><h1>Дані</h1>';
        echo '<p>Версія: ' . (int) ($cache['version'] ?? 0) . '. Редагування — лише в центрі.</p>';
        echo '<table class="widefat"><thead><tr><th>Ключ</th><th>Тип</th><th>Стан</th><th>Значення</th></tr></thead><tbody>';
        foreach ($cache['values'] ?? [] as $v) {
            echo '<tr><td>' . esc_html($v['key'] ?? '') . '</td><td>' . esc_html($v['type'] ?? '') . '</td>'
                . '<td>' . esc_html($v['state'] ?? '') . '</td><td>' . esc_html($v['value'] ?? '') . '</td></tr>';
        }
        echo '</tbody></table></div>';
    }

    public function renderInsert(): void
    {
        $cache = get_option('dbm_cache');
        $cache = is_array($cache) ? $cache : ['values' => []];
        $builder = new SnippetBuilder($this->settings->shortcode);
        echo '<div class="wrap"><h1>Вставка</h1><table class="widefat"><thead><tr><th>Ключ</th><th>Шорткод</th><th>tel</th><th>PHP</th></tr></thead><tbody>';
        foreach ($cache['values'] ?? [] as $v) {
            $s = $builder->forKey((string) ($v['key'] ?? ''));
            echo '<tr><td>' . esc_html($v['key'] ?? '') . '</td><td><code>' . esc_html($s['shortcode']) . '</code></td>'
                . '<td><code>' . esc_html($s['tel']) . '</code></td><td><code>' . esc_html($s['php']) . '</code></td></tr>';
        }
        echo '</tbody></table></div>';
    }

    public function renderSettings(): void
    {
        echo '<div class="wrap"><h1>Налаштування</h1><form method="post" action="options.php">';
        settings_fields('dbm');
        $o = get_option('dbm_settings');
        $o = is_array($o) ? $o : [];
        $field = function (string $key, string $label) use ($o): void {
            $val = esc_attr((string) ($o[$key] ?? ''));
            echo '<p><label>' . esc_html($label) . '<br><input type="text" name="dbm_settings[' . $key . ']" value="' . $val . '" size="60"></label></p>';
        };
        $field('bridge_url', 'URL DataBridge');
        $field('site_token', 'Токен сайта');
        $field('signing_secret', 'Секрет підпису даних');
        $field('ping_secret', 'Секрет пінга');
        $field('shortcode', 'Назва шорткода (нейтральна)');
        $field('css_class', 'CSS-клас виводу');
        submit_button();
        echo '</form></div>';
    }
}
```

- [ ] **Step 3: Прогнати юніти (переконатися, що нічого не зламано)**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit`
Expected: усі зелені (WP-класи не вантажаться в юнітах).

- [ ] **Step 4: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): WP-обв'язка (шорткод/REST/cron) і 3 сторінки адмінки"
```

---

### Task 11: Інтеграція — реальний WordPress, два сценарії-приймання

**Files:**
- Modify: `10-Projects/DBManager/code/docker-compose.yml` (сервіси `wordpress`, `wpcli`)
- Create: `code/plugin/bin/install-wp.sh`
- Create: `code/plugin/bin/scenario.php` (WP-CLI eval-file)

Перевіряє §8/§10: (A) «bridge мертвий → сайт з кешу» (рендер без жодного звернення до bridge); (B) «плагін видалили → значення показуються» (mu-фолбек).

- [ ] **Step 1: compose-сервіси WordPress + WP-CLI**

У `docker-compose.yml` додати:
```yaml
  wordpress:
    image: wordpress:php8.4-apache
    environment:
      WORDPRESS_DB_HOST: mysql
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      WORDPRESS_DB_NAME: dbmanager_wp
    volumes:
      - ./plugin/dbmanager:/var/www/html/wp-content/plugins/dbmanager
      - ./plugin/shared:/var/www/html/wp-content/plugins/dbmanager-shared
      - wp-data:/var/www/html
    depends_on:
      - mysql

  wpcli:
    image: wordpress:cli-php8.4
    environment:
      WORDPRESS_DB_HOST: mysql
      WORDPRESS_DB_USER: root
      WORDPRESS_DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      WORDPRESS_DB_NAME: dbmanager_wp
    volumes:
      - ./plugin:/plugin
      - wp-data:/var/www/html
    depends_on:
      - wordpress
```
І в `volumes:` додати `wp-data:`. У сервіс `mysql` (з плану 1.1) переконатися, що БД `dbmanager_wp` створюється (або створити її в install-скрипті через `wp db query`).

- [ ] **Step 2: install-wp.sh**

`code/plugin/bin/install-wp.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
# Чекаємо БД і ставимо WP «з нуля» для інтеграційного прогону.
wp db create || true
wp core install --url=http://localhost --title=DBM --admin_user=admin \
  --admin_password=admin --admin_email=a@a.dev --skip-email
# mu-plugin фолбек + його самодостатня копія ядра рендеру.
mkdir -p wp-content/mu-plugins
cp /plugin/mu/dbmanager-fallback.php wp-content/mu-plugins/dbmanager-fallback.php
cp /plugin/shared/render-core.php   wp-content/mu-plugins/render-core.php
# Основний плагін бачить shared/ на рівень вище — лінкуємо.
ln -sf /var/www/html/wp-content/plugins/dbmanager-shared \
       /var/www/html/wp-content/plugins/shared || true
wp plugin activate dbmanager
```

- [ ] **Step 3: scenario.php (WP-CLI eval-file)**

`code/plugin/bin/scenario.php`:
```php
<?php
// Запуск: wp eval-file /plugin/bin/scenario.php

// Засіваємо кеш як «отримане від bridge» (без реальної мережі).
update_option('dbm_cache', [
    'site' => 'domen.ua', 'version' => 4,
    'values' => [['key' => 'phone_ua_1', 'state' => 'ok', 'value' => '+380441234567']],
], true);
update_option('dbm_settings', ['shortcode' => 'dbm', 'css_class' => 'val']);

// Сценарій A: bridge мертвий — рендер тільки з кешу, жодного HTTP.
$a = do_shortcode('[dbm key="phone_ua_1"]');
echo "A:" . $a . "\n";

// Сценарій B: видаляємо основний плагін — має лишитися mu-фолбек.
WP_CLI::runcommand('plugin deactivate dbmanager');
WP_CLI::runcommand('plugin delete dbmanager');
// Перевантажуємо PHP-процес неможливо в одному eval — тому B перевіряємо окремою командою (Step 4).
echo "B-pending\n";
```

- [ ] **Step 4: Прогнати інтеграцію**

Run (з `code/`):
```bash
docker compose up -d wordpress mysql
docker compose run --rm wpcli bash /plugin/bin/install-wp.sh
# Сценарій A (плагін активний, рендер із кешу):
docker compose run --rm wpcli wp eval-file /plugin/bin/scenario.php
# Сценарій B (після видалення плагіна mu-фолбек рендерить той самий шорткод):
docker compose run --rm wpcli wp eval 'echo do_shortcode(@"[dbm key=@"phone_ua_1@"]");'
```
Expected: A друкує `A:<span class="val">+380441234567</span>`; команда B (після `plugin delete`) друкує той самий `<span class="val">+380441234567</span>` — значення показуються без основного плагіна. (Лапки в `wp eval` екранувати під свою оболонку.)

- [ ] **Step 5: Зафіксувати спостереження**

Якщо A або B не дають значення — діагностуй: A → шлях `shared/render-core.php` від плагіна; B → чи скопійовано `render-core.php` у `mu-plugins/` і чи `dbm_cache` пережив `plugin delete` (має, бо не чіпаємо опцію). Виправляй код/скрипт, не «підганяй» очікування.

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/plugin 10-Projects/DBManager/code/docker-compose.yml
git commit -m "dbmanager(plugin): інтеграційні сценарії «bridge мертвий» і «плагін видалено»"
```

---

### Task 12: render-core — гео-видимість за країною

**Files:**
- Modify: `code/plugin/shared/render-core.php`
- Test: `code/plugin/tests/Unit/RenderCoreGeoTest.php`

Гео = видимість (контракт компілятора Core: кожне значення несе `geo`, напр. `['UA']` чи `['WORLD']`). Значення показується, якщо `geo` порожній, містить `WORLD`, або містить детектовану країну (`$opts['country']`, дефолт `WORLD`). Жодного JS — фільтрація у тій самій чистій функції.

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/RenderCoreGeoTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use PHPUnit\Framework\TestCase;

class RenderCoreGeoTest extends TestCase
{
    protected function setUp(): void
    {
        require_once __DIR__ . '/../../shared/render-core.php';
    }

    private function payload(): array
    {
        return ['values' => [
            ['key' => 'phone_ua_1', 'state' => 'ok', 'value' => '+380441234567', 'geo' => ['UA']],
            ['key' => 'tg_brand', 'state' => 'ok', 'value' => 'https://t.me/brand', 'geo' => ['WORLD']],
        ]];
    }

    public function test_country_specific_value_shown_for_matching_country(): void
    {
        $this->assertStringContainsString('+380441234567',
            dbm_render_from_cache($this->payload(), 'phone_ua_1', ['country' => 'UA']));
    }

    public function test_country_specific_value_hidden_for_other_country(): void
    {
        $this->assertSame('',
            dbm_render_from_cache($this->payload(), 'phone_ua_1', ['country' => 'PL']));
    }

    public function test_world_value_shown_for_any_country(): void
    {
        $this->assertStringContainsString('t.me/brand',
            dbm_render_from_cache($this->payload(), 'tg_brand', ['country' => 'PL']));
    }

    public function test_unknown_country_defaults_to_world_only(): void
    {
        // Без країни (дефолт WORLD): універсальні значення видно, країнні — ні.
        $this->assertSame('', dbm_render_from_cache($this->payload(), 'phone_ua_1', []));
        $this->assertStringContainsString('t.me/brand',
            dbm_render_from_cache($this->payload(), 'tg_brand', []));
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter RenderCoreGeoTest`
Expected: FAIL (країнне значення поки рендериться завжди).

- [ ] **Step 3: Додати гео-фільтр у `dbm_render_from_cache` (після перевірки state, перед `$raw`)**

У `code/plugin/shared/render-core.php` додати перед рядком `$raw = (string) ($value['value'] ?? '');`:
```php
        $country = $opts['country'] ?? 'WORLD';
        $geo = $value['geo'] ?? [];
        if ($geo !== [] && ! in_array('WORLD', $geo, true) && ! in_array($country, $geo, true)) {
            return ''; // не для цієї країни
        }
```

- [ ] **Step 4: Прогнати — зелений (гео + старі)**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter "RenderCore"`
Expected: PASS (RenderCoreTest 5 + RenderCoreFormatTest 2 + RenderCoreGeoTest 4).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): гео-видимість значень за країною (PHP, без JS)"
```

---

### Task 13: Core — публікація MaxMind-бази у bridge

**Files:**
- Modify: `code/core/config/services.php` (`bridge.geodb_url`)
- Create: `code/core/config/geo.php`
- Modify: `code/core/.env.example`
- Create: `code/core/app/Services/Publishing/GeoDatabasePublisher.php`
- Create: `code/core/app/Console/Commands/PublishGeoDatabase.php`
- Test: `code/core/tests/Feature/GeoDatabasePublisherTest.php`

- [ ] **Step 1: Написати тест**

`code/core/tests/Feature/GeoDatabasePublisherTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Services\Publishing\GeoDatabasePublisher;
use Illuminate\Support\Facades\Http;
use Tests\TestCase;

class GeoDatabasePublisherTest extends TestCase
{
    protected function setUp(): void
    {
        parent::setUp();
        config([
            'services.bridge.geodb_url' => 'https://bridge.local/api/internal/geodb',
            'services.bridge.publish_secret' => 'pub-secret',
        ]);
    }

    public function test_publishes_signed_database_bytes_with_sha(): void
    {
        Http::fake(['*' => Http::response(['stored_sha' => '...'], 200)]);
        $bytes = random_bytes(64);
        $tmp = tempnam(sys_get_temp_dir(), 'mmdb');
        file_put_contents($tmp, $bytes);

        $ok = app(GeoDatabasePublisher::class)->publish($tmp);

        $this->assertTrue($ok);
        Http::assertSent(function ($request) use ($bytes) {
            $sig = hash_hmac('sha256', $request->body(), 'pub-secret');

            return $request->url() === 'https://bridge.local/api/internal/geodb'
                && $request->body() === $bytes
                && $request->hasHeader('X-Signature', $sig)
                && $request->hasHeader('X-Geodb-Sha256', hash('sha256', $bytes));
        });
        @unlink($tmp);
    }

    public function test_returns_false_when_file_missing(): void
    {
        Http::fake();
        $this->assertFalse(app(GeoDatabasePublisher::class)->publish('/no/such.mmdb'));
        Http::assertNothingSent();
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=GeoDatabasePublisherTest`
Expected: FAIL.

- [ ] **Step 3: Конфіг**

У `core/config/services.php` у блок `'bridge'` додати: `'geodb_url' => env('BRIDGE_GEODB_URL'),`.
`core/config/geo.php`:
```php
<?php

return [
    'database_path' => env('GEO_DATABASE_PATH', storage_path('app/geo/GeoLite2-Country.mmdb')),
];
```
У `core/.env.example` додати: `BRIDGE_GEODB_URL=` і `GEO_DATABASE_PATH=`.

- [ ] **Step 4: Сервіс**

`code/core/app/Services/Publishing/GeoDatabasePublisher.php`:
```php
<?php

namespace App\Services\Publishing;

use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Log;

class GeoDatabasePublisher
{
    /** Надіслати .mmdb у bridge з HMAC і sha256. true — прийнято. */
    public function publish(string $path): bool
    {
        if (! is_file($path)) {
            return false;
        }

        $url = (string) config('services.bridge.geodb_url');
        $secret = (string) config('services.bridge.publish_secret');
        if ($url === '' || $secret === '') {
            return false;
        }

        $bytes = (string) file_get_contents($path);

        try {
            $response = Http::withHeaders([
                'Content-Type' => 'application/octet-stream',
                'X-Signature' => hash_hmac('sha256', $bytes, $secret),
                'X-Geodb-Sha256' => hash('sha256', $bytes),
            ])->withBody($bytes, 'application/octet-stream')->post($url);
        } catch (\Throwable $e) {
            Log::warning('Geodb publish failed', ['error' => $e->getMessage()]);

            return false;
        }

        return $response->successful();
    }
}
```

- [ ] **Step 5: Команда**

`code/core/app/Console/Commands/PublishGeoDatabase.php`:
```php
<?php

namespace App\Console\Commands;

use App\Services\Publishing\GeoDatabasePublisher;
use Illuminate\Console\Command;

class PublishGeoDatabase extends Command
{
    protected $signature = 'geodb:publish';

    protected $description = 'Опублікувати MaxMind-базу у DataBridge';

    public function handle(GeoDatabasePublisher $publisher): int
    {
        $ok = $publisher->publish((string) config('geo.database_path'));
        $this->line($ok ? 'Geodb опубліковано' : 'Geodb НЕ опубліковано');

        return $ok ? self::SUCCESS : self::FAILURE;
    }
}
```

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm core php artisan test --filter=GeoDatabasePublisherTest`
Expected: PASS (2 tests). Then: `docker compose run --rm core php artisan test` — усі зелені.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager: публікація MaxMind-бази з Core у DataBridge"
```

---

### Task 14: Bridge — приймання і віддача MaxMind-бази

**Files:**
- Create: `code/bridge/database/migrations/2026_06_14_000001_create_geo_databases.php`
- Create: `code/bridge/app/Models/GeoDatabase.php`
- Create: `code/bridge/app/Http/Controllers/Api/GeoIngestController.php`
- Create: `code/bridge/app/Http/Controllers/Api/GeoServeController.php`
- Modify: `code/bridge/routes/api.php`
- Test: `code/bridge/tests/Feature/GeoDatabaseTest.php`

Bridge зберігає байти бази в одному рядку (`longBlob`) + sha256; ingest захищений HMAC (publish secret), serve — по токену сайта (база спільна для всіх), з `ETag: "<sha>"`, `If-None-Match`→304 і `X-Signature` тіла.

- [ ] **Step 1: Написати тести**

`code/bridge/tests/Feature/GeoDatabaseTest.php`:
```php
<?php

namespace Tests\Feature;

use App\Models\PublishedSite;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class GeoDatabaseTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        config([
            'services.publish.secret' => 'pub',
            'services.data.signing_secret' => 'sign',
        ]);
    }

    private function ingest(string $bytes, ?string $secret = null)
    {
        return $this->call('POST', '/api/internal/geodb', [], [], [], [
            'CONTENT_TYPE' => 'application/octet-stream',
            'HTTP_X_SIGNATURE' => hash_hmac('sha256', $bytes, $secret ?? 'pub'),
            'HTTP_X_GEODB_SHA256' => hash('sha256', $bytes),
        ], $bytes);
    }

    public function test_ingest_rejects_bad_signature(): void
    {
        $this->ingest('AABB', 'wrong')->assertStatus(401);
    }

    public function test_ingest_stores_and_serve_returns_signed_bytes_with_etag(): void
    {
        $bytes = random_bytes(32);
        $this->ingest($bytes)->assertOk();

        PublishedSite::factory()->create(['token_hash' => hash('sha256', 'tok')]);

        $response = $this->call('GET', '/api/v1/geodb', [], [], [], [
            'HTTP_X_SITE_TOKEN' => 'tok',
        ]);

        $response->assertOk();
        $this->assertSame($bytes, $response->getContent());
        $response->assertHeader('ETag', '"' . hash('sha256', $bytes) . '"');
        $response->assertHeader('X-Signature', hash_hmac('sha256', $bytes, 'sign'));
    }

    public function test_serve_returns_304_when_sha_matches(): void
    {
        $bytes = random_bytes(16);
        $this->ingest($bytes)->assertOk();
        PublishedSite::factory()->create(['token_hash' => hash('sha256', 'tok')]);

        $this->call('GET', '/api/v1/geodb', [], [], [], [
            'HTTP_X_SITE_TOKEN' => 'tok',
            'HTTP_IF_NONE_MATCH' => '"' . hash('sha256', $bytes) . '"',
        ])->assertStatus(304);
    }

    public function test_serve_unknown_token_unauthorized(): void
    {
        $this->call('GET', '/api/v1/geodb', [], [], [], ['HTTP_X_SITE_TOKEN' => 'nope'])
            ->assertStatus(401);
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm bridge php artisan test --filter=GeoDatabaseTest`
Expected: FAIL.

- [ ] **Step 3: Міграція + модель**

`code/bridge/database/migrations/2026_06_14_000001_create_geo_databases.php`:
```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('geo_databases', function (Blueprint $table) {
            $table->id();
            $table->string('sha256', 64)->unique();
            $table->longText('bytes'); // base64 байтів бази
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('geo_databases');
    }
};
```

`code/bridge/app/Models/GeoDatabase.php`:
```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class GeoDatabase extends Model
{
    protected $guarded = [];
}
```

- [ ] **Step 4: Контролери**

`code/bridge/app/Http/Controllers/Api/GeoIngestController.php`:
```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\GeoDatabase;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\Request;

class GeoIngestController extends Controller
{
    public function __invoke(Request $request): JsonResponse
    {
        $secret = config('services.publish.secret');
        abort_if(! $secret, 500, 'Publish secret is not configured');

        $bytes = $request->getContent();
        abort_unless(
            hash_equals(hash_hmac('sha256', $bytes, $secret), (string) $request->header('X-Signature')),
            401,
            'Invalid signature'
        );

        $sha = hash('sha256', $bytes);
        GeoDatabase::updateOrCreate(['sha256' => $sha], ['bytes' => base64_encode($bytes)]);
        // Лишаємо лише найновішу.
        GeoDatabase::where('sha256', '!=', $sha)->delete();

        return response()->json(['stored_sha' => $sha]);
    }
}
```

`code/bridge/app/Http/Controllers/Api/GeoServeController.php`:
```php
<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;
use App\Models\GeoDatabase;
use App\Models\PublishedSite;
use Illuminate\Http\Request;
use Illuminate\Http\Response;

class GeoServeController extends Controller
{
    public function __invoke(Request $request): Response
    {
        $rawToken = (string) $request->header('X-Site-Token');
        abort_if($rawToken === '', 401, 'Missing token');
        abort_unless(
            PublishedSite::where('token_hash', hash('sha256', $rawToken))->exists(),
            401,
            'Unknown token'
        );

        $db = GeoDatabase::latest('id')->first();
        abort_unless($db, 404, 'No geo database');

        $etag = '"' . $db->sha256 . '"';
        if (trim((string) $request->header('If-None-Match')) === $etag) {
            return response('', 304)->header('ETag', $etag);
        }

        abort_if(! config('services.data.signing_secret'), 500);

        $bytes = base64_decode($db->bytes);
        $signature = hash_hmac('sha256', $bytes, (string) config('services.data.signing_secret'));

        return response($bytes, 200)
            ->header('Content-Type', 'application/octet-stream')
            ->header('ETag', $etag)
            ->header('X-Signature', $signature);
    }
}
```

- [ ] **Step 5: Маршрути**

У `code/bridge/routes/api.php` додати:
```php
use App\Http\Controllers\Api\GeoIngestController;
use App\Http\Controllers\Api\GeoServeController;

Route::post('/internal/geodb', GeoIngestController::class);
Route::get('/v1/geodb', GeoServeController::class)->middleware(['bridge.log', 'throttle:30,1']);
```

- [ ] **Step 6: Прогнати — зелений**

Run: `docker compose run --rm bridge php artisan test --filter=GeoDatabaseTest`
Expected: PASS (4 tests). Then: `docker compose run --rm bridge php artisan test` — усі зелені.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/bridge
git commit -m "dbmanager(bridge): приймання і віддача MaxMind-бази по токену"
```

---

### Task 15: Plugin — синхронізація MaxMind-бази

**Files:**
- Create: `code/plugin/dbmanager/src/Geo/GeoDbClient.php` (порт)
- Create: `code/plugin/dbmanager/src/Geo/GeoDbStore.php` (порт)
- Create: `code/plugin/dbmanager/src/Geo/GeoDbSynchronizer.php`
- Create: `code/plugin/tests/Support/FakeGeoDbClient.php`
- Create: `code/plugin/tests/Support/InMemoryGeoDbStore.php`
- Test: `code/plugin/tests/Unit/GeoDbSynchronizerTest.php`

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/GeoDbSynchronizerTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Geo\GeoDbSynchronizer;
use DBM\Sync\PayloadVerifier;
use DBM\Tests\Support\FakeGeoDbClient;
use DBM\Tests\Support\InMemoryGeoDbStore;
use PHPUnit\Framework\TestCase;

class GeoDbSynchronizerTest extends TestCase
{
    private const SIGN = 'sign';

    private function ok(string $bytes): array
    {
        return ['status' => 200, 'body' => $bytes, 'etag' => '"' . hash('sha256', $bytes) . '"',
                'signature' => hash_hmac('sha256', $bytes, self::SIGN)];
    }

    private function sync(FakeGeoDbClient $c, InMemoryGeoDbStore $s): GeoDbSynchronizer
    {
        return new GeoDbSynchronizer($c, $s, new PayloadVerifier(),
            url: 'https://bridge/api/v1/geodb', token: 'tok', signingSecret: self::SIGN);
    }

    public function test_downloads_and_stores_verified_database(): void
    {
        $store = new InMemoryGeoDbStore();
        $bytes = random_bytes(20);

        $this->assertTrue($this->sync(new FakeGeoDbClient($this->ok($bytes)), $store)->sync());
        $this->assertSame($bytes, $store->bytes());
    }

    public function test_bad_signature_keeps_existing(): void
    {
        $store = new InMemoryGeoDbStore();
        $store->put('OLD');
        $bad = $this->ok(random_bytes(8));
        $bad['signature'] = 'tampered';

        $this->assertFalse($this->sync(new FakeGeoDbClient($bad), $store)->sync());
        $this->assertSame('OLD', $store->bytes());
    }

    public function test_304_is_noop(): void
    {
        $store = new InMemoryGeoDbStore();
        $store->put('KEEP');
        $client = new FakeGeoDbClient(['status' => 304, 'body' => '', 'etag' => '', 'signature' => '']);

        $this->assertFalse($this->sync($client, $store)->sync());
        $this->assertSame('KEEP', $store->bytes());
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter GeoDbSynchronizerTest`
Expected: FAIL.

- [ ] **Step 3: Порти, фейки, синхронізатор**

`code/plugin/dbmanager/src/Geo/GeoDbClient.php`:
```php
<?php

namespace DBM\Geo;

interface GeoDbClient
{
    /** @return array{status:int, body:string, signature:string, etag:string} */
    public function fetch(string $url, string $token, ?string $ifNoneMatchSha): array;
}
```

`code/plugin/dbmanager/src/Geo/GeoDbStore.php`:
```php
<?php

namespace DBM\Geo;

interface GeoDbStore
{
    public function sha(): ?string;  // sha наявної бази або null

    public function bytes(): ?string;

    public function put(string $bytes): void;

    public function path(): string;  // шлях до файла бази для рідера
}
```

`code/plugin/tests/Support/FakeGeoDbClient.php`:
```php
<?php

namespace DBM\Tests\Support;

use DBM\Geo\GeoDbClient;

class FakeGeoDbClient implements GeoDbClient
{
    public array $calls = [];

    public function __construct(private array $response) {}

    public function fetch(string $url, string $token, ?string $ifNoneMatchSha): array
    {
        $this->calls[] = compact('url', 'token', 'ifNoneMatchSha');

        return $this->response;
    }
}
```

`code/plugin/tests/Support/InMemoryGeoDbStore.php`:
```php
<?php

namespace DBM\Tests\Support;

use DBM\Geo\GeoDbStore;

class InMemoryGeoDbStore implements GeoDbStore
{
    private ?string $bytes = null;

    public function sha(): ?string
    {
        return $this->bytes === null ? null : hash('sha256', $this->bytes);
    }

    public function bytes(): ?string
    {
        return $this->bytes;
    }

    public function put(string $bytes): void
    {
        $this->bytes = $bytes;
    }

    public function path(): string
    {
        return '/tmp/fake.mmdb';
    }
}
```

`code/plugin/dbmanager/src/Geo/GeoDbSynchronizer.php`:
```php
<?php

namespace DBM\Geo;

use DBM\Sync\PayloadVerifier;

class GeoDbSynchronizer
{
    public function __construct(
        private GeoDbClient $client,
        private GeoDbStore $store,
        private PayloadVerifier $verifier,
        private string $url,
        private string $token,
        private string $signingSecret,
    ) {}

    /** true — базу оновлено. Битий підпис/304/помилка — лишаємо наявну. */
    public function sync(): bool
    {
        $response = $this->client->fetch($this->url, $this->token, $this->store->sha());

        if ($response['status'] !== 200) {
            return false;
        }
        if (! $this->verifier->verify($response['body'], $response['signature'], $this->signingSecret)) {
            return false;
        }

        $this->store->put($response['body']);

        return true;
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter GeoDbSynchronizerTest`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): синхронізація MaxMind-бази з bridge (fail-soft)"
```

---

### Task 16: Plugin — GeoDetector (CF → MaxMind → WORLD) + вшити країну в рендер

**Files:**
- Modify: `code/plugin/composer.json` (`maxmind-db/reader`)
- Create: `code/plugin/dbmanager/src/Geo/CountryLookup.php` (порт лукапу IP)
- Create: `code/plugin/dbmanager/src/Geo/GeoDetector.php`
- Create: `code/plugin/dbmanager/src/Geo/MaxMindCountryLookup.php` (адаптер рідера)
- Modify: `code/plugin/dbmanager/src/Wp/Plugin.php` (детект країни → у шорткод/`dbm_get`)
- Test: `code/plugin/tests/Unit/GeoDetectorTest.php`

- [ ] **Step 1: Написати тест**

`code/plugin/tests/Unit/GeoDetectorTest.php`:
```php
<?php

namespace DBM\Tests\Unit;

use DBM\Geo\CountryLookup;
use DBM\Geo\GeoDetector;
use PHPUnit\Framework\TestCase;

class GeoDetectorTest extends TestCase
{
    private function detector(?string $lookupResult): GeoDetector
    {
        $lookup = new class($lookupResult) implements CountryLookup {
            public function __construct(private ?string $r) {}

            public function country(string $ip): ?string
            {
                return $this->r;
            }
        };

        return new GeoDetector($lookup);
    }

    public function test_prefers_cloudflare_header(): void
    {
        $country = $this->detector('PL')->detect(['CF-IPCountry' => 'UA'], '1.2.3.4');
        $this->assertSame('UA', $country);
    }

    public function test_falls_back_to_lookup_when_no_header(): void
    {
        $this->assertSame('PL', $this->detector('PL')->detect([], '1.2.3.4'));
    }

    public function test_world_when_nothing_resolves(): void
    {
        $this->assertSame('WORLD', $this->detector(null)->detect([], '1.2.3.4'));
    }

    public function test_ignores_cloudflare_placeholder_xx(): void
    {
        // CF віддає "XX" для невідомих/анонімайзерів — це не країна.
        $this->assertSame('PL', $this->detector('PL')->detect(['CF-IPCountry' => 'XX'], '1.2.3.4'));
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter GeoDetectorTest`
Expected: FAIL.

- [ ] **Step 3: Порт + детектор**

`code/plugin/dbmanager/src/Geo/CountryLookup.php`:
```php
<?php

namespace DBM\Geo;

interface CountryLookup
{
    /** ISO-код країни за IP або null. */
    public function country(string $ip): ?string;
}
```

`code/plugin/dbmanager/src/Geo/GeoDetector.php`:
```php
<?php

namespace DBM\Geo;

class GeoDetector
{
    public function __construct(private CountryLookup $lookup) {}

    /** Країна: CF-IPCountry → лукап IP → 'WORLD'. $headers — асоц. масив заголовків. */
    public function detect(array $headers, string $ip): string
    {
        $cf = strtoupper(trim((string) ($headers['CF-IPCountry'] ?? '')));
        if ($cf !== '' && $cf !== 'XX' && ctype_alpha($cf)) {
            return $cf;
        }

        $byIp = $this->lookup->country($ip);

        return $byIp !== null && $byIp !== '' ? strtoupper($byIp) : 'WORLD';
    }
}
```

- [ ] **Step 4: Прогнати — зелений**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit --filter GeoDetectorTest`
Expected: PASS (4 tests).

- [ ] **Step 5: Адаптер рідера + залежність**

Run (з `code/`): `docker compose run --rm plugin composer require maxmind-db/reader`

`code/plugin/dbmanager/src/Geo/MaxMindCountryLookup.php`:
```php
<?php

namespace DBM\Geo;

use MaxMind\Db\Reader;

class MaxMindCountryLookup implements CountryLookup
{
    public function __construct(private string $dbPath) {}

    public function country(string $ip): ?string
    {
        if (! is_file($this->dbPath) || $ip === '') {
            return null;
        }
        try {
            $reader = new Reader($this->dbPath);
            $record = $reader->get($ip);
            $reader->close();
        } catch (\Throwable) {
            return null;
        }

        return $record['country']['iso_code'] ?? null;
    }
}
```

- [ ] **Step 6: Вшити країну в рендер (модифікація `Wp/Plugin.php` із Task 10)**

У `registerShortcode()` визначати країну один раз і передавати у вивід. Замінити тіло `dbm_get` і колбек шорткода так, щоб додавати `country`:
```php
        $detector = new \DBM\Geo\GeoDetector(
            new \DBM\Geo\MaxMindCountryLookup((string) (get_option('dbm_geodb_path') ?: ''))
        );
        $country = $detector->detect(
            ['CF-IPCountry' => $_SERVER['HTTP_CF_IPCOUNTRY'] ?? ''],
            $_SERVER['HTTP_CF_CONNECTING_IP'] ?? ($_SERVER['REMOTE_ADDR'] ?? '')
        );

        add_shortcode($s->shortcode, function ($atts) use ($country) {
            $atts = shortcode_atts(['key' => '', 'format' => ''], $atts);

            return dbm_get((string) $atts['key'], ['format' => (string) $atts['format'], 'country' => $country]);
        });
```
(`dbm_get` уже додає `class`; тепер прокидаємо ще `country`. mu-фолбек робить те саме — оновити його колбек у `mu/dbmanager-fallback.php` аналогічно, з тим самим детектором.)

Також додати реєстрацію WP-cron `dbm_geodb_sync` (тижнево) у `register()`, що кличе `GeoDbSynchronizer->sync()` через `WpHttpGeoDbClient`/`FileGeoDbStore` (адаптери за зразком Task 5; шлях бази зберігати в `dbm_geodb_path`).

- [ ] **Step 7: Прогнати юніти + Commit**

Run: `docker compose run --rm plugin ./vendor/bin/phpunit`
Expected: усі зелені.
```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): гео-детектор (CF→MaxMind→WORLD) і вшивання країни в рендер"
```

---

### Task 17: Інтеграція — гео-сценарій на реальному WordPress

**Files:**
- Create: `code/plugin/bin/scenario-geo.php`

- [ ] **Step 1: Сценарій**

`code/plugin/bin/scenario-geo.php`:
```php
<?php
// Запуск: CF-IPCountry імітуємо через $_SERVER перед do_shortcode.

update_option('dbm_cache', ['site' => 'domen.ua', 'version' => 5, 'values' => [
    ['key' => 'phone_ua_1', 'state' => 'ok', 'value' => '+380441234567', 'geo' => ['UA']],
    ['key' => 'tg_brand', 'state' => 'ok', 'value' => 'https://t.me/brand', 'geo' => ['WORLD']],
]], true);
update_option('dbm_settings', ['shortcode' => 'dbm', 'css_class' => 'val']);

$_SERVER['HTTP_CF_IPCOUNTRY'] = 'UA';
echo "UA phone:" . do_shortcode('[dbm key="phone_ua_1"]') . "\n";
echo "UA tg:" . do_shortcode('[dbm key="tg_brand"]') . "\n";

$_SERVER['HTTP_CF_IPCOUNTRY'] = 'PL';
echo "PL phone:" . do_shortcode('[dbm key="phone_ua_1"]') . "\n";
echo "PL tg:" . do_shortcode('[dbm key="tg_brand"]') . "\n";
```

- [ ] **Step 2: Прогнати**

Run (з `code/`):
```bash
docker compose run --rm wpcli wp eval-file /plugin/bin/scenario-geo.php
```
Expected: `UA phone:` містить `+380441234567`; `UA tg:` містить `t.me/brand`; `PL phone:` порожній (UA-значення приховане для PL); `PL tg:` містить `t.me/brand` (WORLD видно всюди).

- [ ] **Step 3: Commit**

```bash
git add 10-Projects/DBManager/code/plugin
git commit -m "dbmanager(plugin): інтеграційний гео-сценарій (UA vs WORLD за CF-IPCountry)"
```

---

### Task 18: Фінал — прогони, людський гайд, контекст, пуш

**Files:**
- Create: `50-Guides/AI/Гайд — WP-плагін DBManager.md`
- Modify: `10-Projects/DBManager/DBManager — Контекст.md`
- Modify: `10-Projects/DBManager/DBManager — План.md`

- [ ] **Step 1: Повні прогони трьох сервісів**

Run:
```bash
docker compose run --rm plugin ./vendor/bin/phpunit
docker compose run --rm core php artisan test
docker compose run --rm bridge php artisan test
```
Expected: усі зелені, 0 failures (core і bridge — з новими гео-тестами).

- [ ] **Step 2: Повторити інтеграційні сценарії (A/B + гео)**

Run: повтор Task 11 Step 4 і Task 17 Step 2. Expected: кеш-рендер без bridge, mu-фолбек після видалення, гео UA vs WORLD — усі очікувані виводи.

- [ ] **Step 3: Людський гайд (обов'язково — AGENTS.md §10)**

`50-Guides/AI/Гайд — WP-плагін DBManager.md`: що це (read-only клієнт центру, рендер суто PHP без JS), коли ставити (новий сайт парку), як підключити (створити сайт у центрі → токен → вкладка «Налаштування»: URL bridge, токен, секрети, нейтральні шорткод/CSS-клас), гео (вмикається саме собою через `CF-IPCountry`; для кешу — вимога варіювати за країною), як перевірити (вкладка «Дані» показує версію і значення; шорткод `[dbm key="..."]` рендерить; «зв'язок втрачено → показуються збережені дані»). Послатися на `[[DBManager — Дизайн]]` §8 і `[[2026-06-14 — Гео-рендер плагіна на PHP без JS|ADR гео]]`.

- [ ] **Step 4: Оновити контекст і план**

У `DBManager — Контекст.md`: стан → «Плани 1.1–1.3 виконано: Core + DataBridge + WP-плагін (з гео на PHP)»; наступний крок → «Етап 2 — адмінка, ролі, імпорт» (гео-рендер уже зроблено в 1.3). У `DBManager — План.md` (Етап 1) відмітити план 1.3 виконаним; у Етапі 2 зняти «JS-рендер гео-значень» (зроблено на PHP у 1.3, лишити хіба роздачу/оновлення MaxMind як експлуатацію).

- [ ] **Step 5: Commit + push**

```bash
git add 10-Projects/DBManager 50-Guides/AI
git commit -m "dbmanager: завершено план 1.3 — WP-плагін DBManager з гео на PHP"
git push
```

---

## Покриття спеки планом 1.3 (self-review)

- §8 плагін — вкладки «Дані»/«Вставка»/«Налаштування» → Task 10 (AdminPages); шорткод `[dbm]` + `dbm_get()` + `tel:` → Tasks 3, 10; анти-fingerprint (нейтральні класи/шорткод, екранування, без маркерів) → Tasks 3, 7, 10.
- §8 живучість — кеш у `wp_options` із `autoload`, не чиститься при деактивації (тільки `clear()`) → Task 5, 10 (deactivation hook не чіпає кеш); mu-plugin фолбек самодостатній → Tasks 9, 11.
- §5 доставка — приймач push-пінга `/wp-json/dbm/v1/ping` (HMAC) → Tasks 8, 10; добова звірка `If-None-Match`→304 → Task 6 (`reconcile`), Task 10 (WP-cron daily); рендер завжди з кешу, нуль звернень до bridge на показі → Tasks 3, 11A.
- §9 безпека — перевірка `X-Signature` відповіді даних і пінгів (fail-closed на порожній секрет) → Tasks 4, 6, 8; токен/секрети в налаштуваннях → Task 7, 10; HTTPS-виклики через `wp_remote_get` → Task 5.
- §10 відмови — «bridge недоступний → кеш безстроково» → Task 6 (fail-soft), Task 11A; «пінг не доставлено → добова звірка» → Task 6; «плагін видалено → значення показуються» → Task 11B.
- §8 гео + §4 гео-видимість (PHP, без JS — [[2026-06-14 — Гео-рендер плагіна на PHP без JS|ADR]]) — видимість значення за країною (`country` ∈ `geo` або `WORLD`) → Task 12; джерело країни CF-IPCountry→MaxMind→WORLD → Task 16; роздача MaxMind-бази центр→bridge→плагін → Tasks 13 (Core), 14 (bridge), 15 (plugin sync); вшивання країни в шорткод/`dbm_get`/mu → Task 16; гео-сценарій на WP → Task 17.
- §12 тестування — юніт чистої логіки (рендер/гео/підпис/синхронізатори) → Tasks 2–9, 12, 15, 16; тести плагіна «кеш переживає видалення», «mu без основного плагіна», «bridge мертвий → сайт працює», гео UA vs WORLD → Tasks 11, 17.
- **Поза 1.3 (свідомо):** власний сервер оновлень плагіна, блоки Gutenberg — пізніші етапи; роздача MaxMind тут є, але автоматизація оновлення базі MaxMind у центрі (cron на стороні Core) — експлуатація/Етап 4. Перевірка підпису даних/geodb потребує, щоб bridge serve додавав `X-Signature` (є з плану 1.2). Вимога деплою: page-cache варіюється за `CF-IPCountry` (див. ADR).
