# План імплементації 2.1 — Адмінка Core: оболонка + грід «Значення»

> **✅ ВИКОНАНО 2026-06-15.** Tasks 1–7: Breeze(Livewire)+Tailwind, оболонка `/admin` за макетом v5, `SiteGridReader`, грід «Значення» з failover-статусами/областю дії, фільтри (тип/гео/статус/пошук), перемикач сайта. Core 84 тести, 0 fail. Доступ: http://localhost:8001 (`admin@dbmanager.local`/`admin`). Людський гайд: `50-Guides/AI/Гайд — Адмінка DBManager.md`. Далі — план 2.2 (панель слота).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перший зріз адмінки DBManager Core — захищена входом оболонка (хедер із хлібними крихтами, смужка контексту, бічна навігація) і дата-центричний грід «Значення»: для обраного сайта показує всі значення, згруповані за типами, зі статусом (failover), областю дії (група / ✎ сайта), гео й к-стю резервів, плюс фільтри (тип/гео/статус/пошук). Усе за затвердженим макетом.

**Architecture:** Адмінка живе в наявному Laravel-додатку `core/` (поряд із моделями/сервісами планів 1.1–1.3). Стек — Livewire + Blade + Tailwind (сервер-driven, мінімум JS; внутрішній інструмент, не обмежений анти-fingerprint). Дані гріда готує окремий read-model `App\Admin\SiteGridReader` (комбінує `SitePayloadCompiler`-логіку вирішення failover із областю дії та лічильником резервів) — він юніт-тестується без UI; Livewire-компонент лише рендерить його вивід. Редагування слота (панель) — окремий План 2.2.

**Tech Stack:** PHP 8.4, Laravel (версія як у `core/`), Livewire 3, Tailwind, Laravel Breeze (Livewire stack) для входу/каркасу, Vite, PHPUnit + Livewire::test.

**Спека (джерело правди):** `DBManager — Дизайн.md` §7 (адмінка, UI затверджено) і §2 (ролі). Макет: `.superpowers/brainstorm/2289-1781202386/content/grid-failover-v5.html` (структура хедера/контексту/сайдбару/гріда; палітра «A — димчасто-синій» `--acc:#54708c`, фон `#f4f5f3`, білі картки, контурні Lucide-іконки, пастельні статуси sage/amber/brick).

**Робоча директорія docker-команд:** `C:/Dev/Meweek/10-Projects/DBManager/code` (bash). Тести: `docker compose run --rm core php artisan test`. Збірка фронту: через контейнер core (`npm`); якщо npm немає в образі — використати образ `node:22` з монтуванням `core/` (за зразком composer:2 у планах 1.x). Git — з кореня репо, коміти `dbmanager(admin): ...`, тільки шляхи під `10-Projects/DBManager/code/core`, без push (крім фінального таска).

**Поза скоупом 2.1 (свідомо):** панель слота — перегляд/редагування ланцюга номерів, pin, режим повернення, вичерпання, прив'язані месенджери, Save→аудит+публікація (План 2.2); дашборд, інциденти, аудит-стрічка, bulk-дії з прев'ю/відкатом, ролі/права (різання за сайтами/діями), імпорт CSV/XLSX (плани 2.3+). У 2.1 вхід — один сидований супер-адмін без рольової матриці.

**Структура файлів (`core/`):**

```
core/
  app/Admin/SiteGridReader.php           # read-model гріда (групи рядків per сайт)
  app/Livewire/ValuesGrid.php            # Livewire-компонент гріда + фільтри + контекст
  resources/views/livewire/values-grid.blade.php
  resources/views/components/layouts/admin.blade.php   # оболонка: хедер/крихти/контекст/сайдбар
  resources/views/admin/icons.blade.php  # <symbol> спрайт Lucide (з макета)
  routes/web.php                         # /admin (auth) → ValuesGrid
  database/seeders/AdminUserSeeder.php   # супер-адмін
  tailwind.config.js / resources/css/app.css           # палітра + компонентні класи
  tests/Feature/Admin/SiteGridReaderTest.php
  tests/Feature/Admin/ValuesGridTest.php
```

---

### Task 1: Breeze (Livewire) — вхід, Tailwind, сидований адмін

**Files:**
- Create (Breeze): auth-маршрути/в'юхи, `tailwind.config.js`, `vite.config.js`, `resources/css/app.css`, `resources/js/app.js`
- Create: `core/database/seeders/AdminUserSeeder.php`
- Modify: `core/database/seeders/DatabaseSeeder.php`
- Test: `core/tests/Feature/Admin/AuthGateTest.php`

- [ ] **Step 1: Встановити Breeze (Livewire stack)**

Run (з `code/`; composer через образ composer:2, як у планах 1.x, якщо його нема в образі core):
```bash
docker compose run --rm core composer require laravel/breeze --dev
docker compose run --rm core php artisan breeze:install livewire --no-interaction
```
Expected: з'являються `routes/auth.php`, `app/Livewire/Actions`, `resources/views/livewire/...auth`, `tailwind.config.js`, оновлений `resources/views/layouts/app.blade.php`. (Якщо `breeze:install` тягне npm — збірку зробимо в Step 4.)

- [ ] **Step 2: Сидер супер-адміна**

`core/database/seeders/AdminUserSeeder.php`:
```php
<?php

namespace Database\Seeders;

use App\Models\User;
use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\Hash;

class AdminUserSeeder extends Seeder
{
    public function run(): void
    {
        User::firstOrCreate(
            ['email' => 'admin@dbmanager.local'],
            ['name' => 'Супер-адмін', 'password' => Hash::make('admin')]
        );
    }
}
```
У `DatabaseSeeder::run()` додати `$this->call(AdminUserSeeder::class);`.

- [ ] **Step 3: Тест воріт автентифікації**

`core/tests/Feature/Admin/AuthGateTest.php`:
```php
<?php

namespace Tests\Feature\Admin;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class AuthGateTest extends TestCase
{
    use RefreshDatabase;

    public function test_admin_route_requires_login(): void
    {
        $this->get('/admin')->assertRedirect('/login');
    }

    public function test_logged_in_user_reaches_admin(): void
    {
        $this->actingAs(User::factory()->create())
            ->get('/admin')->assertOk();
    }
}
```
(Маршрут `/admin` додається в Task 2 — поки тест падатиме на 404/redirect; це нормально, доводимо його в Task 2. Для RED тут достатньо першого тесту: незалогінений → /login.)

- [ ] **Step 4: Зібрати фронт і прогнати**

Run:
```bash
docker compose run --rm core npm install
docker compose run --rm core npm run build
docker compose run --rm core php artisan test --filter=AuthGateTest
```
Expected: `test_admin_route_requires_login` — PASS (Breeze дає `/login`); другий — поки FAIL (нема `/admin`), доведемо в Task 2. Якщо `npm` нема в образі core — запустити через `docker run --rm -v "$PWD/core:/app" -w /app node:22 npm install && ... npm run build`.

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): Breeze (Livewire) — вхід, Tailwind, сидований супер-адмін"
```

---

### Task 2: Оболонка адмінки — layout + маршрут /admin

**Files:**
- Create: `core/resources/views/admin/icons.blade.php`
- Create: `core/resources/views/components/layouts/admin.blade.php`
- Modify: `core/tailwind.config.js`, `core/resources/css/app.css`
- Modify: `core/routes/web.php`
- Test: `core/tests/Feature/Admin/AuthGateTest.php` (уже є — тепер обидва зелені)

Оболонка точно за макетом v5: хедер (логотип «DBManager Core», хлібні крихти `Усі сайти › Група › Сайт`, пошук/дзвіночок/користувач), смужка контексту, бічна навігація (Дашборд / **Значення** / Сайти і групи / Інциденти / Аудит / Доступи й токени). Активний пункт — «Значення».

- [ ] **Step 1: Палітра у Tailwind**

У `tailwind.config.js` в `theme.extend.colors` додати:
```js
colors: {
  acc: { DEFAULT: '#54708c', bg: '#e9eef3', tx: '#3a526b', bd: '#c8d4de' },
  canvas: '#f4f5f3',
  ink: '#2f3438',
  mut: '#7d837f',
  ok: { bg: '#e9f0ec', tx: '#52705f' },
  warn: { bg: '#f5eedd', tx: '#96752f' },
  bad: { bg: '#f3e5e2', tx: '#96514a' },
},
```

- [ ] **Step 2: Спрайт іконок**

`core/resources/views/admin/icons.blade.php` — `<svg style="display:none">…</svg>` зі `<symbol>` з макета (скопіювати блок іконок `#i-phone, #i-msg, #i-tag, #i-bell, #i-search, #i-user, #i-pin, #i-alert, #i-ban, #i-grip, #i-globe, #i-grid, #i-sites, #i-audit, #i-key` дослівно з `grid-failover-v5.html`, рядки 78–94).

- [ ] **Step 3: Layout оболонки**

`core/resources/views/components/layouts/admin.blade.php` — Blade-компонент-layout із `{{ $slot }}` у `.main`, що відтворює структуру макета на Tailwind:
```blade
<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>DBManager Core</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
    @livewireStyles
</head>
<body class="bg-canvas text-ink text-[13px]">
    @include('admin.icons')
    <div class="max-w-[1280px] mx-auto my-4 border border-[#e3e5e1] rounded-xl overflow-hidden bg-canvas">
        {{-- Хедер --}}
        <div class="bg-white border-b border-[#e3e5e1] px-[18px] py-3 flex justify-between items-center">
            <div class="flex gap-3 items-center">
                <b class="text-[14px]">DBManager Core</b>
                {{ $breadcrumb ?? '' }}
            </div>
            <div class="flex gap-4 items-center text-mut">
                <span>⌘K пошук</span>
                <a href="#" class="relative">@svg('bell')<span class="bg-[#a85c52] text-white rounded-lg text-[10px] px-1.5">{{ $incidents ?? 0 }}</span></a>
                <span>{{ auth()->user()?->name }}</span>
                <form method="POST" action="{{ route('logout') }}">@csrf<button class="underline">вихід</button></form>
            </div>
        </div>
        {{ $context ?? '' }}
        <div class="flex">
            {{-- Сайдбар --}}
            <nav class="w-[170px] shrink-0 bg-white border-r border-[#e3e5e1] py-3">
                @php($nav = [['Дашборд','grid',false],['Значення','tag',true],['Сайти і групи','sites',false],['Інциденти','alert',false],['Аудит','audit',false],['Доступи й токени','key',false]])
                @foreach($nav as [$label,$icon,$active])
                    <div @class(['px-[18px] py-2 flex gap-2.5 items-center text-mut', 'bg-acc-bg border-r-[3px] border-acc text-acc-tx font-bold' => $active])>
                        @svg($icon) {{ $label }}
                    </div>
                @endforeach
            </nav>
            <div class="flex-1 min-w-0">{{ $slot }}</div>
        </div>
    </div>
    @livewireScripts
</body>
</html>
```
І невеликий Blade-директив-хелпер `@svg('name')` для `<svg class="w-[15px] h-[15px] stroke-[#7d837f] fill-none"><use href="#i-name"/></svg>` — оголосити в `app/Providers/AppServiceProvider.php` через `Blade::directive('svg', fn($e) => "<svg class=\"inline w-4 h-4 align-[-3px] stroke-[#7d837f] fill-none\" style=\"stroke-width:1.7\"><use href=\"#i-<?php echo trim($e,\"'\\\" \"); ?>\"/></svg>")`. (Якщо директива ускладнює — замінити на `<x-svg name="..."/>` компонент; головне — один спосіб вставки іконки.)

- [ ] **Step 4: Маршрут /admin**

У `core/routes/web.php`:
```php
use App\Livewire\ValuesGrid;
use Illuminate\Support\Facades\Route;

Route::middleware('auth')->group(function () {
    Route::get('/admin', ValuesGrid::class)->name('admin.values');
});
```
(Компонент `ValuesGrid` — Task 4; для проходження AuthGateTest на цьому етапі тимчасово можна вказати закриту Blade-в'юху `admin.placeholder`, що використовує layout `admin`, і замінити на ValuesGrid у Task 4. Якщо так — створити `resources/views/admin/placeholder.blade.php` з `<x-layouts.admin>OK</x-layouts.admin>` і маршрут на неї.)

- [ ] **Step 5: Прогнати — зелено**

Run: `docker compose run --rm core php artisan test --filter=AuthGateTest`
Expected: обидва PASS (незалогінений → /login; залогінений → 200 на /admin).

- [ ] **Step 6: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): оболонка (хедер/контекст/сайдбар) за макетом + маршрут /admin"
```

---

### Task 3: SiteGridReader — read-model рядків гріда

**Files:**
- Create: `core/app/Admin/SiteGridReader.php`
- Test: `core/tests/Feature/Admin/SiteGridReaderTest.php`

Для сайта повертає рядки, згруповані за типом, кожен: `key`, `type`, `geo[]`, `state` (ok/on_reserve/pinned/exhausted/hidden), `value`, `scope` ('group'|'site'), `reserves` (к-сть резервів у слоті, напр. «3» або «0/2»). Вирішення failover — через `SlotResolver` (як у компіляторі); область дії — site-override якщо існує site-scoped `DataValue` з тим ключем, інакше group.

- [ ] **Step 1: Написати тест**

`core/tests/Feature/Admin/SiteGridReaderTest.php`:
```php
<?php

namespace Tests\Feature\Admin;

use App\Admin\SiteGridReader;
use App\Models\DataValue;
use App\Models\GeoTag;
use App\Models\Site;
use App\Models\SiteGroup;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\Support\BuildsSlots;
use Tests\TestCase;

class SiteGridReaderTest extends TestCase
{
    use RefreshDatabase;
    use BuildsSlots;

    public function test_groups_rows_by_type_with_scope_and_status(): void
    {
        $group = SiteGroup::factory()->create();
        $site = Site::factory()->for($group, 'group')->create();

        // груповий телефон-слот, активний
        [$slot, $entries] = $this->slotWithNumbers(['active', 'active']);
        $slot->dataValue->update(['key' => 'phone_ua_1', 'scope_type' => 'group', 'scope_id' => $group->id]);
        $slot->dataValue->geoTags()->attach(GeoTag::where('code', 'UA')->first());

        // власне (site-override) значення-ціна
        DataValue::factory()->forSite($site)->create([
            'key' => 'price_basic', 'content' => ['value' => '1200'],
        ]);

        $rows = app(SiteGridReader::class)->forSite($site);

        $phones = collect($rows['phone'] ?? []);
        $phone = $phones->firstWhere('key', 'phone_ua_1');
        $this->assertSame('group', $phone['scope']);
        $this->assertSame('ok', $phone['state']);
        $this->assertSame(['UA'], $phone['geo']);
        $this->assertSame(2, $phone['reserves']);

        $price = collect($rows['price'] ?? [])->firstWhere('key', 'price_basic');
        $this->assertSame('site', $price['scope']);
        $this->assertSame('1200', $price['value']);
    }

    public function test_site_override_marked_site_scope(): void
    {
        $group = SiteGroup::factory()->create();
        $site = Site::factory()->for($group, 'group')->create();
        DataValue::factory()->forGroup($group)->create(['key' => 'addr', 'content' => ['value' => 'Г']]);
        DataValue::factory()->forSite($site)->create(['key' => 'addr', 'content' => ['value' => 'С']]);

        $rows = app(SiteGridReader::class)->forSite($site);
        $addr = collect($rows['text'] ?? [])->firstWhere('key', 'addr');

        $this->assertSame('site', $addr['scope']);
        $this->assertSame('С', $addr['value']);
    }
}
```
(Якщо фабрика типу за замовчуванням не `text` — у тесті виставити потрібний `ofType(...)`, звіривши з наявними фабриками планів 1.1.)

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=SiteGridReaderTest`
Expected: FAIL (нема класу).

- [ ] **Step 3: Реалізація**

`core/app/Admin/SiteGridReader.php`:
```php
<?php

namespace App\Admin;

use App\Models\DataValue;
use App\Models\Site;
use App\Services\Failover\SlotResolver;
use Illuminate\Support\Collection;

class SiteGridReader
{
    public function __construct(private SlotResolver $resolver) {}

    /** @return array<string, array<int, array>> тип → рядки */
    public function forSite(Site $site): array
    {
        $with = ['type', 'geoTags', 'phoneSlot.entries'];

        $group = $site->site_group_id
            ? DataValue::with($with)->where('status', 'active')
                ->where('scope_type', 'group')->where('scope_id', $site->site_group_id)->get()
            : collect();
        $own = DataValue::with($with)->where('status', 'active')
            ->where('scope_type', 'site')->where('scope_id', $site->id)->get();

        $ownKeys = $own->pluck('key')->flip();
        $effective = $group->keyBy('key')->merge($own->keyBy('key'));

        $rows = [];
        foreach ($effective as $value) {
            $row = $this->row($value, $ownKeys->has($value->key) ? 'site' : 'group');
            $rows[$value->type->code][] = $row;
        }

        return $rows;
    }

    private function row(DataValue $value, string $scope): array
    {
        $geo = $value->geoTags->pluck('code')->all() ?: ['WORLD'];
        $base = [
            'key' => $value->key,
            'type' => $value->type->code,
            'geo' => $geo,
            'scope' => $scope,
            'reserves' => 0,
            'state' => 'ok',
            'value' => $value->content['value'] ?? null,
        ];

        $slot = $value->phoneSlot;
        if ($slot) {
            $resolved = $this->resolver->resolve($slot);
            $base['reserves'] = max(0, $slot->entries->count() - 1);
            $base['state'] = $resolved->visible ? $resolved->state : 'hidden';
            $base['value'] = $resolved->visible ? $resolved->number : null;
        }

        return $base;
    }
}
```
(Звірити назви `ResolvedSlot` (`visible`/`state`/`number`) з `app/Services/Failover/ResolvedSlot.php`; за потреби підкоригувати поля.)

- [ ] **Step 4: Прогнати — зелено**

Run: `docker compose run --rm core php artisan test --filter=SiteGridReaderTest`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): read-model гріда значень (статус, область дії, резерви)"
```

---

### Task 4: ValuesGrid — Livewire-компонент гріда

**Files:**
- Create: `core/app/Livewire/ValuesGrid.php`
- Create: `core/resources/views/livewire/values-grid.blade.php`
- Modify: `core/routes/web.php` (на ValuesGrid, прибрати placeholder)
- Test: `core/tests/Feature/Admin/ValuesGridTest.php`

- [ ] **Step 1: Написати тест**

`core/tests/Feature/Admin/ValuesGridTest.php`:
```php
<?php

namespace Tests\Feature\Admin;

use App\Livewire\ValuesGrid;
use App\Models\DataValue;
use App\Models\Site;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Livewire\Livewire;
use Tests\TestCase;

class ValuesGridTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->actingAs(User::factory()->create());
    }

    public function test_shows_site_values_grouped_by_type(): void
    {
        $site = Site::factory()->create();
        DataValue::factory()->forSite($site)->create(['key' => 'price_basic', 'content' => ['value' => '1200']]);

        Livewire::test(ValuesGrid::class, ['site' => $site->id])
            ->assertSee('price_basic')
            ->assertSee('1200');
    }

    public function test_defaults_to_first_site_when_none_given(): void
    {
        $site = Site::factory()->create(['domain' => 'domen.ua']);
        DataValue::factory()->forSite($site)->create(['key' => 'k1']);

        Livewire::test(ValuesGrid::class)
            ->assertSee('domen.ua')
            ->assertSee('k1');
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=ValuesGridTest`
Expected: FAIL.

- [ ] **Step 3: Компонент**

`core/app/Livewire/ValuesGrid.php`:
```php
<?php

namespace App\Livewire;

use App\Admin\SiteGridReader;
use App\Models\Site;
use Livewire\Component;

class ValuesGrid extends Component
{
    public ?int $site = null;

    public function mount(?int $site = null): void
    {
        $this->site = $site ?? Site::query()->orderBy('id')->value('id');
    }

    public function render()
    {
        $site = $this->site ? Site::with('group')->find($this->site) : null;
        $rows = $site ? app(SiteGridReader::class)->forSite($site) : [];

        return view('livewire.values-grid', ['siteModel' => $site, 'rows' => $rows])
            ->layout('components.layouts.admin');
    }
}
```

- [ ] **Step 4: В'юха гріда (за макетом)**

`core/resources/views/livewire/values-grid.blade.php` — відтворює `.main` із макета: смужка фільтрів (поки статичні chips — інтерактив у Task 5), потім групи за типами з лічильниками й рядками (checkbox-плейсхолдер, key, гео, статус-бейдж + значення, к-сть резервів, бейдж області). Статуси → пастельні класи `ok/warn/bad`. Приклад групи:
```blade
<div class="p-3.5">
    <div class="flex gap-2 items-center text-mut text-xs mb-1.5">
        <span class="border border-[#dfe3e0] bg-white rounded-lg px-2.5 py-1">Тип ▾</span>
        <span class="border border-acc bg-acc-bg text-acc-tx rounded-lg px-2.5 py-1 font-semibold">Гео ▾</span>
        <span class="border border-[#dfe3e0] bg-white rounded-lg px-2.5 py-1">Статус ▾</span>
        <span class="border border-[#dfe3e0] bg-white rounded-lg px-2.5 py-1">@svg('search') Пошук…</span>
    </div>

    @php($labels = ['phone' => ['Телефони','phone'], 'messenger' => ['Месенджери','msg'], 'price' => ['Ціни','tag'], 'text' => ['Текст','tag']])
    @foreach($rows as $type => $items)
        <div class="mt-3.5">
            <div class="text-[11px] uppercase tracking-wide text-mut mb-1 flex gap-1.5 items-center">
                @svg(($labels[$type][1] ?? 'tag')) {{ $labels[$type][0] ?? $type }} · {{ count($items) }}
            </div>
            @foreach($items as $r)
                <div class="grid grid-cols-[24px_minmax(105px,1fr)_44px_minmax(210px,1.7fr)_50px_96px] gap-2.5 items-center px-2.5 py-2.5 mt-1 bg-white border border-[#e3e5e1] rounded-[10px]">
                    <span>☐</span>
                    <span class="font-medium">{{ $r['key'] }}</span>
                    <span class="text-mut">{{ implode(',', $r['geo']) }}</span>
                    <span>
                        @php($st = ['ok'=>'ok','on_reserve'=>'warn','pinned'=>'warn','exhausted'=>'bad','hidden'=>'bad'][$r['state']] ?? 'ok')
                        <span class="inline-flex items-center gap-1.5 rounded-md px-2 py-0.5 font-semibold text-xs bg-{{ $st }}-bg text-{{ $st }}-tx">● {{ $r['state'] }}</span>
                        <span class="text-mut">{{ $r['value'] }}</span>
                    </span>
                    <span class="text-mut">{{ $r['reserves'] ?: '—' }}</span>
                    <span>
                        @if($r['scope'] === 'site')
                            <span class="inline-block bg-[#eef1ee] border border-dashed border-[#aeb6b0] rounded-md px-2 text-[11px] text-[#5a625d]">✎ цього сайта</span>
                        @else
                            <span class="text-mut">Група</span>
                        @endif
                    </span>
                </div>
            @endforeach
        </div>
    @endforeach
</div>
```
(Tailwind має бачити класи `bg-ok-bg`/`text-ok-tx` тощо — додати їх у safelist `tailwind.config.js` `safelist: ['bg-ok-bg','text-ok-tx','bg-warn-bg','text-warn-tx','bg-bad-bg','text-bad-tx']`, бо вони складаються динамічно.)

Хедерні слоти layout (`$breadcrumb`, `$context`) передати через `<x-slot>` у в'юсі гріда: крихти `Усі сайти › Група: {{ $siteModel?->group?->name }} › Сайт: {{ $siteModel?->domain }}` і смужку контексту з домену.

- [ ] **Step 5: Маршрут на ValuesGrid**

У `routes/web.php` замінити placeholder-в'юху на `Route::get('/admin', ValuesGrid::class)`.

- [ ] **Step 6: Прогнати — зелено + збірка**

Run:
```bash
docker compose run --rm core npm run build
docker compose run --rm core php artisan test --filter="ValuesGridTest|AuthGateTest"
```
Expected: усі PASS.

- [ ] **Step 7: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): грід «Значення» (Livewire) за макетом v5"
```

---

### Task 5: Фільтри гріда (тип / гео / статус / пошук)

**Files:**
- Modify: `core/app/Livewire/ValuesGrid.php`
- Modify: `core/resources/views/livewire/values-grid.blade.php`
- Test: `core/tests/Feature/Admin/ValuesGridFilterTest.php`

- [ ] **Step 1: Написати тест**

`core/tests/Feature/Admin/ValuesGridFilterTest.php`:
```php
<?php

namespace Tests\Feature\Admin;

use App\Livewire\ValuesGrid;
use App\Models\DataValue;
use App\Models\GeoTag;
use App\Models\Site;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Livewire\Livewire;
use Tests\TestCase;

class ValuesGridFilterTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->actingAs(User::factory()->create());
    }

    public function test_search_filters_by_key(): void
    {
        $site = Site::factory()->create();
        DataValue::factory()->forSite($site)->create(['key' => 'phone_ua_1']);
        DataValue::factory()->forSite($site)->create(['key' => 'price_basic']);

        Livewire::test(ValuesGrid::class, ['site' => $site->id])
            ->set('search', 'price')
            ->assertSee('price_basic')
            ->assertDontSee('phone_ua_1');
    }

    public function test_geo_filter_limits_rows(): void
    {
        $site = Site::factory()->create();
        $ua = DataValue::factory()->forSite($site)->create(['key' => 'k_ua']);
        $ua->geoTags()->attach(GeoTag::where('code', 'UA')->first());
        DataValue::factory()->forSite($site)->create(['key' => 'k_world']); // WORLD за замовчуванням

        Livewire::test(ValuesGrid::class, ['site' => $site->id])
            ->set('geo', 'UA')
            ->assertSee('k_ua')
            ->assertDontSee('k_world');
    }
}
```

- [ ] **Step 2: Запустити — падає**

Run: `docker compose run --rm core php artisan test --filter=ValuesGridFilterTest`
Expected: FAIL.

- [ ] **Step 3: Додати фільтри у компонент**

У `ValuesGrid` додати публічні `?string $search = null; ?string $type = null; ?string $geo = null; ?string $status = null;` і застосувати їх до рядків після `forSite()` (фільтрувати масив: за `str_contains(key, search)`, `type`, `in_array(geo, row.geo)`, `state == status`). Винести фільтрацію в приватний метод `applyFilters(array $rows): array`. Прив'язати у в'юсі `wire:model.live` до chips/інпута пошуку.

- [ ] **Step 4: Прогнати — зелено**

Run: `docker compose run --rm core php artisan test --filter=ValuesGridFilterTest`
Expected: PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): фільтри гріда (тип/гео/статус/пошук)"
```

---

### Task 6: Перемикач контексту сайта/групи

**Files:**
- Modify: `core/app/Livewire/ValuesGrid.php`
- Modify: `core/resources/views/livewire/values-grid.blade.php`
- Test: `core/tests/Feature/Admin/SiteContextTest.php`

Хлібні крихти-дропдауни дозволяють обрати сайт; зміна сайта оновлює грід і смужку контексту. (Перегляд на рівні групи — спрощено: показ значень обраного сайта; повноцінний «вид усієї групи» — пізніший план.)

- [ ] **Step 1: Написати тест**

`core/tests/Feature/Admin/SiteContextTest.php`:
```php
<?php

namespace Tests\Feature\Admin;

use App\Livewire\ValuesGrid;
use App\Models\DataValue;
use App\Models\Site;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Livewire\Livewire;
use Tests\TestCase;

class SiteContextTest extends TestCase
{
    use RefreshDatabase;

    public function test_switching_site_reloads_values(): void
    {
        $this->actingAs(User::factory()->create());
        $a = Site::factory()->create(['domain' => 'a.ua']);
        $b = Site::factory()->create(['domain' => 'b.ua']);
        DataValue::factory()->forSite($a)->create(['key' => 'only_a']);
        DataValue::factory()->forSite($b)->create(['key' => 'only_b']);

        Livewire::test(ValuesGrid::class, ['site' => $a->id])
            ->assertSee('only_a')->assertDontSee('only_b')
            ->set('site', $b->id)
            ->assertSee('only_b')->assertDontSee('only_a');
    }
}
```

- [ ] **Step 2: Запустити — падає / зелено**

Run: `docker compose run --rm core php artisan test --filter=SiteContextTest`
Expected: якщо `set('site', …)` уже оновлює render (бо `site` публічна й render читає її) — PASS одразу. Якщо ні — додати `updatedSite()` або переконатися, що render бере поточний `$this->site`. Довести до PASS.

- [ ] **Step 3: Крихти-дропдаун у в'юсі**

У хедерному слоті крихт відрендерити `<select wire:model.live="site">` з усіма сайтами (домен як підпис), згрупованими за групою (optgroup). Смужку контексту оновити з обраного сайта. (Дизайн: `seg`/`seg.cur` стилі — відтворити Tailwind-класами.)

- [ ] **Step 4: Прогнати — зелено + збірка**

Run:
```bash
docker compose run --rm core npm run build
docker compose run --rm core php artisan test --filter="Admin"
```
Expected: усі адмін-тести зелені.

- [ ] **Step 5: Commit**

```bash
git add 10-Projects/DBManager/code/core
git commit -m "dbmanager(admin): перемикач контексту сайта в хлібних крихтах"
```

---

### Task 7: Фінал — прогони, гайд, контекст/план, пуш

**Files:**
- Create: `50-Guides/AI/Гайд — Адмінка DBManager.md`
- Modify: `10-Projects/DBManager/DBManager — Контекст.md`, `DBManager — План.md`

- [ ] **Step 1: Повний прогін core + збірка фронту**

Run:
```bash
docker compose run --rm core npm run build
docker compose run --rm core php artisan test
```
Expected: усі зелені (наявні 51 + нові адмін-тести), 0 failures.

- [ ] **Step 2: Візуальна перевірка (скриншот за бажанням)**

Run: `docker compose up -d core` і відкрити `http://localhost:8001/admin` (порт core з compose), увійти `admin@dbmanager.local / admin`. Переконатися: оболонка, грід згрупований за типами, статуси кольорові, фільтри й перемикач сайта працюють. (Порт звірити з `docker-compose.yml`.)

- [ ] **Step 3: Людський гайд (AGENTS.md §10)**

`50-Guides/AI/Гайд — Адмінка DBManager.md`: що це (внутрішня адмінка Core), як зайти (URL, логін), що вже вміє (перегляд значень сайта за типами зі статусами failover, фільтри, перемикання сайта), що далі (редагування слота — 2.2). Послатися на `[[DBManager — Дизайн]]` §7.

- [ ] **Step 4: Оновити контекст і план**

У `DBManager — Контекст.md`: додати, що почато Етап 2 — адмінка (план 2.1: оболонка + грід «Значення» read-only). У `DBManager — План.md` (Етап 2) відмітити зроблене й лишити наступним План 2.2 (панель слота: редагування + Save→аудит+публікація).

- [ ] **Step 5: Commit + push**

```bash
git add 10-Projects/DBManager 50-Guides/AI
git commit -m "dbmanager: завершено план 2.1 — оболонка адмінки і грід значень"
git push
```

---

## Покриття спеки планом 2.1 (self-review)

- §7 каркас (дата-центричний грід — головний екран) → Tasks 4–5 (ValuesGrid + фільтри); read-model → Task 3.
- §7 контекст (хлібні крихти, смужка контексту, область дії в рядку) → Tasks 2, 4, 6.
- §7 структура (бічна навігація, групування за типами з лічильниками, фільтри тип/гео/статус/пошук) → Tasks 2, 4, 5.
- §7 стиль (фон #f4f5f3, білі картки, акцент #54708c, Lucide-іконки, пастельні статуси) → Task 2 (палітра/іконки/layout) + Task 4 (рядки/бейджі) — усе за макетом `grid-failover-v5.html`.
- §2/§9 вхід (пароль, без 2FA) → Task 1 (Breeze, сидований супер-адмін); ліміт спроб входу й аудит входів — борг Етапу 3.
- **Поза 2.1 (свідомо):** панель слота (редагування) — План 2.2; дашборд/інциденти/аудит-стрічка → плани 2.x; bulk з прев'ю/відкатом, ролі/права, імпорт CSV/XLSX → плани 2.x; різання прав за сайтами/діями — Етап 2 пізніше. Грід поки лише читає; усі дії редагування — у наступних планах.
