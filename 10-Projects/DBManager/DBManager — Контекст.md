---
tags: [dbmanager, контекст]
---

# DBManager — Контекст

**Оновлено:** 2026-06-20
**Стан:** Фаза дослідження нових типів даних завершена. Написано 4 дослідницькі документи. Реалізація — наступний крок (чекає підтвердження архітектури «Адреси»).

## Нові типи даних (соцмережі + адреси) — дослідження
- Робочі типи сьогодні: `phone`, `messenger`, `price`. `address`/`social`/`text` засіяні у `value_types`, але **не підключені** до механізмів (провалюються в generic-текст; create-валідація `ValueEditor.php:146` їх блокує).
- Карта механізмів: [[DATA_TYPES_MAP]] (тип → 8 механізмів, реєстр хардкод-списків, матриця готовності).
- Аудит масових змін: [[BULK_EDIT_AUDIT]] (8 точок плутанини, пріоритети P0–P2, стан підсвічування).
- Рішення «Адреса»: [[ADDRESS_DECISION]] — обрано **A+** (структуровано + `value`-дзеркало); «вільний текст» закриває окремий тип `text`.
- План інтеграції: [[NEW_TYPES_PLAN]] (social ≈ messenger без слотів; address структуровано; контракт тестів на всі механізми).
- Розвідка: фан-аут 8 агентів + критик, перехресно звірено читанням коду. Скрипт: `recon-workflow.js`.

## Наступні кроки
1. Підтвердити архітектуру «Адреси» (A+) і політику bulk для неї.
2. Реалізувати інкрементально (фундамент → social → address → підсвічування), TDD, зелені тести, регресія phone/price/messenger.
3. Закомітити + запушити.

## Незакомічений WIP (не чіпати без потреби)
- `BulkOperations.php` + `BulkOperationsTest.php`: приховування відкочених bulk-сесій зі списку нещодавніх (`getRecentBulkSessions`).

## Раніше зроблене
- Динамічний рендеринг колонок прев'ю масових дій (7 кол. phone, 5 phone_reserve, 6 інші).
- Відображення постраждалих доменів і типів в аудиті; виправлено індикатор «Оновлення...» у сайдбарі.

## Перевірено локально
- `docker compose exec -T core php artisan test` — усі 256 тестів PASS (станом до фази дослідження).
- Код проєкту: `10-Projects/DBManager/code/`.
- Реалізовано динамічний рендеринг колонок у сітці прев'ю масових дій:
  - 7 колонок для телефонів (`phone`), включаючи "Форматування".
  - 5 колонок для резервних телефонів (`phone_reserve`), без "Гео" та "Форматування".
  - 6 колонок для всіх інших типів, приховуючи колонку "Форматування" (оскільки її мають тільки регулярні телефони).
- Виправлено індикатор "Оновлення..." у лівому сайдбарі: перенесено scroll-список в окремий контейнер з `overflow-hidden` для запобігання наповзанню на рамки. Текст оновлення винесено в білу плашку.
- Додано відображення списку постраждалих доменів у стовпчику "Дія" та деталях відновлення (`audit.restored`) в історії аудиту.
- Виправлено відображення типу даних для масових дій (`bulk.*`) у стовпчику "Тип" в аудиті.

## Перевірено локально
- `docker compose exec -T core php artisan test` — усі 256 тестів (Unit & Feature) PASS.
- Додано Feature-тест `test_get_affected_domains` для перевірки визначення постраждалих доменів в `AuditRestorationTest`.
