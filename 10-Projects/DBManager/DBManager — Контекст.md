---
tags: [dbmanager, контекст]
---

# DBManager — Контекст

**Оновлено:** 2026-06-20
**Стан:** Виправлено відображення оверлеїв та сітки в масових діях. Усі тести проходять.

## Актуально
- Код проєкту: `10-Projects/DBManager/code/`.
- Реалізовано 5-колонкову сітку для `phone_reserve` та 7-колонкову для інших типів (без Гео/Форматування для резервів).
- Виправлено `BindingResolutionException` під час відкату масових змін шляхом імпорту `PhoneNumberAssignment` в `AuditRestorer`.
- Покращено пропорційність колонок у таблиці (використано `fr` замість фіксованих `px`), що усуває зжатість елементів.
- Виправлено індикатор "Оновлення..." у лівому сайдбарі: винесено scroll-список в окремий контейнер, а оверлей позиціонується відносно батьківського блоку з `overflow-hidden`. Текст оверлея винесено в білу плашку (`bg-white` та `border`), щоб запобігти наповзанню на текст сайтів.
- Відкати масових операцій тепер повністю приховують скасовані батчі з панелі "Останні масові зміни".

## Перевірено локально
- `docker compose exec -T core php artisan test` — усі 256 тестів (Unit & Feature) PASS.
- Перевірено відкати масових змін, оверлей завантаження та пропорційний розподіл колонок таблиць.
